# AUDIT-2026-09 — Lane B (audit seat), read-only

**Lens:** ROADMAP.md "Principles / invariants" P1-P5 (one-frontend/host-logic-only-in-bridge,
clean hot-reload lifecycle, isolated sub-modules, declare-over-hand-roll, simplicity+surgical) +
the north-star gate (declare over hand-roll; codebase must not lie about itself; removal leaves no
doorless handlers / vacuous tests). Every finding below is verified against source in THIS worktree
(`b-spline-generator-web-addin-lane-b`, branch `lane-b`, HEAD `2102292`) — read + grep, not survey.
Paths relative to `bspline-frame-builder/` unless stated otherwise.

**Date:** 2026-09-17. **Turn:** lane-b turn 1 (A1).

---

## A1 scope — reconcile table (old items, current status)

| Old item | Source | Status this turn | Evidence |
|---|---|---|---|
| B5 — frame-inspector leaks `activeSelectionChanged` across Stop→Start | BUGS_OPEN.md | **STILL OPEN**, confirmed current | `frame-inspector/fusion-inspector.py:686-688` (`run()` adds `sel_handler`) vs `:694-730` (`stop()`) — no `ui.activeSelectionChanged.remove(...)` anywhere in `stop()`. Worse than B5's original text states: `_handlers` (module list, `:10`) is also never cleared in `stop()`, so the Python-side list itself grows unbounded too, not just the Fusion-side subscription. |
| B7 — `selection_items` not force-wiped | BUGS_OPEN.md | **RESOLVED**, confirmed correct | Already marked resolved in E7a (turn 105, commit `c60628b`): `payload_builder.py` + `selection_items.py` were deleted outright (dead, no callers), not wiped. Re-verified this turn: `grep -rn "payload_builder\|selection_items"` over `bspline-frame-builder/` → 0 hits outside docs. |
| B10 — CAM-builder stop() unregisters 1/3 CustomEvents | BUGS_OPEN.md | **not re-verified** (out of A1's actual code-read scope — CAM-builder's internals are A6; A1 only owed the registry question below, answered) | — |
| STANDARDS-AUDIT §1b — per-palette shared-module drift (`expression_coords.py`/`entity_helpers.py` duplicated in frame-inspector/ + template-maker/core/) | STANDARDS-AUDIT.md (July) | **RESOLVED, superseded** | `find frame-inspector template-maker -iname expression_coords.py -o -iname entity_helpers.py` → 0 hits. Both palettes now import package-qualified from `fb_shared.*` only (`frame-inspector/fusion-inspector.py:22`; `template-maker/core/{dimension_hint,offset_hint,relation_hints,template_generator,template_payload,template_payload_builder}.py`). The C4/F8 de-dup (WORK-LOG turns 63-77, S1-S5) fully retired the per-palette copies. |
| STANDARDS-AUDIT §4 — bare `except:` counts (fusion-inspector.py 18, fusion-exporter.py 13, exporter.py 36) | STANDARDS-AUDIT.md (July) | **RESOLVED (counts), one sub-item STILL OPEN** | `grep -cE "except\s*:"` (bare) on all three → **0**. Narrowed to `except Exception:` in WORK-LOG turns 57 (C3a, exporter.py) and 59 (C3b, fusion-inspector.py + fusion-exporter.py + 2 frame-builder files). **BUT** turn 57 itself flagged ~2-3 business-logic loop catch-alls in `exporter.py` that silently skip a whole setup/NC-program on failure (Option B, "not yet blessed") — still present verbatim: `exporter.py:656` (per-setup), `:705` (per-NC-program), `:440`-ish (products deep-scan), all `except Exception: pass`. Not re-flagging as new; citing WORK-LOG turn 57 as prior art, still open. |
| STANDARDS-AUDIT §5 — hardcoded machine paths in fusion-exporter | STANDARDS-AUDIT.md (July) | **STILL OPEN**, confirmed current | `fusion-exporter/fusion-exporter.py:165` — `r'C:\Users\danse\APPS\import-export-template\comparative-audit\Fusion-json'`. `fusion-exporter/exporter.py:86` — `r'C:\Users\danse\APPS\b-spline-generator-web-addin\bspline-frame-builder\fusion-exporter\exported files'` (a default EXPORT output dir nested inside the repo itself — also a "don't ship exported user data next to source" smell, not just portability). |

---

## A1 findings

| ID | add-in | file:line | class | symptom | evidence | fix shape | severity |
|---|---|---|---|---|---|---|---|
| A1-1 | loader (bspline-frame-builder.py) | `bspline-frame-builder.py:123-168` | dead | `_find_related_addin_modules`, `_invoke_addin_action`, `_stop_related_addins`, `_run_related_addins` (46 lines) have **zero callers** anywhere in the repo. The file's own comment at `:394-399` explains WHY: template-maker/fusion-inspector/fusion-exporter were consolidated into this add-in's own run()/stop() lifecycle, making the scan-based approach redundant — but the dead functions were never deleted. | `grep -n` for all 4 names → only their own definitions + the one comment mentioning the first by name. | delete (principle #5: unused abstraction) | L |
| A1-2 | frame-inspector | `frame-inspector/fusion-inspector.py:686-730` | bug / P2-violation | Restates B5 with tighter evidence (see reconcile table) — `stop()` never releases the `activeSelectionChanged` subscription OR clears `_handlers`. Each Stop→Start cycle adds one more live `_SelectionChangedHandler`; N cycles fire N handlers per selection change. | Read `run()` (`:635-692`) + `stop()` (`:694-730`) in full; no `.remove(` call anywhere in the file. | mirror template-maker's pattern (`template-maker/template-maker.py:797-812`, per B5) — `ui.activeSelectionChanged.remove(sel_handler)` + `_handlers.clear()` in `stop()` | M (matches B5) |
| A1-3 | fb_shared | `fb_shared/entity_helpers.py:7-9`, `fb_shared/expression_coords.py:7` | honesty | Both module docstrings assert **"NO callers are switched to this module yet"** / **"no production callers switched yet"** — false today. Every consumer (frame-inspector, template-maker's 6 `core/*.py` files, the test conftest) already imports these package-qualified, confirmed live since WORK-LOG turns 71/73/77 (S3-S5, months ago). Stale S1-era doc left in place through 3 later migration slices. | `grep -rn "fb_shared" frame-inspector template-maker fusion-exporter --include=*.py` → 12 live import sites. | reword the two docstrings to state the migration is complete (S1-S5 done); the `[GATE]`/`[FLAG]` semantic-decision notes inside `entity_helpers.py` were evidently ratified (callers switched) — say so instead of "pending advisor review" | L |
| A1-4 | deploy (DEPLOY_bspline-frame-builder.py) | `DEPLOY_bspline-frame-builder.py:143-144` | dead / honesty | `deploy_template_maker()` — the legacy "separate add-in" deploy target — lists `verify_files` including `"entity_helpers.py"` and `"expression_coords.py"` at template-maker **root**. Neither ever existed there (the July audit had them under `template-maker/core/`; both are now gone entirely, superseded by `fb_shared/`). Same stale-verify-entry class E7a (turn 105) just fixed for `deploy_fusion_inspector()` — missed here because this legacy target wasn't in E7a's scope. Low real-world impact: this target is explicitly marked "legacy... emergency debug only" in the CLI usage text (`:710-712`) and prints a WARNING before running — but every invocation would also print 2 spurious `WARNING: expected source file missing` lines. | `ls template-maker/entity_helpers.py template-maker/expression_coords.py template-maker/core/entity_helpers.py template-maker/core/expression_coords.py` → all 4 "No such file". | drop the 2 dead entries from `verify_files` (mirrors the E7a fix) | L |
| A1-5 | fusion-exporter | `fusion-exporter/fusion-exporter.py:124,143-158` | dead / honesty | `stop()`'s panel-cleanup block targets a legacy panel id, `panels_to_clean = ['FusionIOPanel']` (`:124`), but `run()` (and the parent orchestrator) actually create/use the SHARED panel id `bsplinePanel_<tab>` (`:85-88`). The cleanup loop at `:143-158` can therefore never match anything it creates — it's a no-op that silently relies entirely on the parent's blanket `PANEL_ID`-prefix sweep (`bspline-frame-builder.py` `stop()` step 2) to actually remove the shared panel. Not a live bug (the parent's sweep does the real work) but the code asserts a cleanup responsibility it cannot discharge. | Read `run()` `:75-110` (panel id construction) vs `stop()` `:116-160` (`panels_to_clean` list) side by side; grepped for `FusionIOPanel` — only appears in this one dead list. | delete the dead `panels_to_clean` block, or rename it to match `bsplinePanel` and document that it's redundant with the parent's sweep (either is fine — just stop the false "I clean this up" implication) | L |
| A1-6 | deploy (DEPLOY_bspline-frame-builder.py) | `_deploy_addin`/`deploy_local`, whole-file structural | inefficiency / principle | No orphan-detection: the verify step only checks that files named in `verify_files`/`VERIFY_FILES` match between source and dest. It never diffs "files present in DEST but absent from SOURCE" — so a deleted source file's stale copy can survive indefinitely in the AddIns folder **if** a redeploy ever hits the overlay-fallback path (`clean_dir()` failing its `rmtree` because Fusion has a file locked, per the file's own docstring at `:276-282`). This is exactly the class of bug E7a's ground truth called out by name ("Deploy is an OVERLAY copy... deleting a source file does NOT delete its copy"). **Currently NOT manifesting**: checked the live `%APPDATA%\...\AddIns\bspline-frame-builder\frame-inspector\` folder — only `fusion-inspector.py` present, no `payload_builder.py`/`selection_items.py` residue (a full `rmtree`-then-copy must have run since E7a). Flagging the structural gap, not a live instance. | Read `clean_dir()` (`:275-297`), `copy_overlay()` (`:300-339`), `_deploy_addin()` verify loop (`:113-126`) — none diff dest-only files. `Get-ChildItem` on the live AddIns folder confirmed currently clean. | add an optional post-copy sweep: list dest files with no corresponding source path (excluding known DEST-only artifacts like `build-info.json`, `*.json` handshakes, `.addin-running.lock`) and warn | L |

**Inefficiencies:** none found rising above the items above — `_bootstrap()`'s per-submodule `_force_wipe(_shared_project_names)` call (`bspline-frame-builder.py:264,271,278`) runs the same wipe list 3× per Start (once before each of fusion-exporter/fusion-inspector/template-maker) instead of once; each wipe is a cheap `sys.modules` dict-key scan over a short list, so this is repeated work but not a perf-material one — noting for completeness, not promoting to a finding.

## What's GOOD (per the STANDARDS-AUDIT convention of naming healthy parts, not just gaps)

- **`bspline-frame-builder.py`'s OWN run()/stop() symmetry is clean.** Enumerated what `run()`
  registers vs what `stop()` releases (the explicit ask in NEXT-SESSION for P2/P3):
  - `run()` registers: 1 refresh CustomEvent (session-scoped, intentionally never released — documented
    at `:689-691`); 3 top-level command defs + handlers (`handlers` list); 1 hidden reload command +
    handler; 3 toolbar panels (`bsplinePanel_<tab>` × SolidTab/SketchTab/MillingTab) + their controls;
    5 sub-add-ins' own `run(context)` calls (each sub owns its own resources — P3 isolation).
  - `stop()` releases, in order: `_teardown_submodules()` (delegates to each sub's own `stop()` in
    REVERSE run order, clears sketch/solid handler lists, removes sketch's `documentActivated` sub, nulls
    all sub-module globals) → removes every panel whose id starts with `bsplinePanel` across ALL tabs →
    deletes the 5 command defs → closes 4 named legacy palettes → `handlers.clear()` in `finally` (always
    runs, even on exception).
  - Every sub-module load (`_bootstrap()`) and every sub-module `run()`/`stop()` call is individually
    `try/except`-wrapped (`bspline-frame-builder.py:265-304`, `:667-676`, `:368-376`) — P3 isolation is
    genuinely honored at the orchestrator layer; one bad sub-module cannot take down the others or block
    the rest of teardown.
  - **Verdict: no P2/P3 violation in this file itself.** The one open lifecycle bug (A1-2/B5) is inside a
    sub-module that owns its own cleanup — correctly out of the parent's responsibility per the P3 design.
- `fb_shared/build_info.py` — clean, accurate docstrings, pure-stdlib, never raises, does what it says. No findings.
- `release.py` — matches STANDARDS-AUDIT §5's description exactly (git-push → Cloudflare auto-rebuild;
  never touches the workers directly); step ordering, flag parsing, and the Claude-CLI-with-fallback
  commit-message chain are all straightforward and match their own docstrings.

## Registry question — CAM-builder

**LIVE — loaded by the entry file, not dead, not a separate thing.** ROADMAP.md's one-line "app in one
line" narrative never names it, but that's a doc-completeness gap in ROADMAP, already superseded by
`ARCHITECTURE.md:244` which documents it as the "CAM Studio" sibling palette. Evidence it's live in the
unified add-in exactly like frame-inspector/fusion-exporter/template-maker/stamp-editor:
- `bspline-frame-builder.py:285-293` — `_bootstrap()` wipes `cam_engine`/`cam_utils` then loads
  `CAM-builder/cam-builder.py` as `_cam`.
- `bspline-frame-builder.py:663-676` — `run()`'s consolidated sub-add-in loop calls `_cam.run(context)`.
- `bspline-frame-builder.py:363-376` — `stop()`'s `_teardown_submodules()` tears it down FIRST (reverse
  run order), delegating to its own `stop()`.
- `DEPLOY_bspline-frame-builder.py`'s `deploy_local()` copies the whole `bspline-frame-builder/` tree
  via `copy_overlay` (no CAM-builder exclusion in `SKIP_NAMES`/`SKIP_FILES_EXACT`), so it ships as part
  of the unified deploy.
- Already has its own tracked bug (B10, BUGS_OPEN.md) — further evidence it's an actively-audited live
  surface, not a forgotten/dead one. (B10's internals not re-verified this turn — out of A1 scope,
  queued for A6.)

## What I could not verify

- Whether `exporter.py`'s 2-3 silent-skip catch-alls (`:656`/`:705`/`:440`) have ever actually fired in
  practice (i.e., whether a setup or NC-program has silently gone missing from an export) — static
  read only, no runtime log to check from this read-only lane.
- Whether the live `%APPDATA%\...\AddIns\bspline-frame-builder\` folder's current clean state (checked
  for A1-6) reflects a redeploy that happened AFTER E7a (turn 105) specifically, or some earlier clean
  redeploy that happened to predate the now-deleted files being copied there at all — didn't chase the
  deploy history to disambiguate; either way the folder is clean right now.
- `CAM-builder/` internals (8.8k lines) — confirmed it's loaded (the registry question), did not audit
  its code; queued for A6 per NEXT-SESSION's scope boundary.
- `b-spline-gen/` (`_bs` in the loader) — referenced by the loader but its own code is A5 scope, not
  read this turn beyond the one `_bs.stop(None)` delegation call site.

---

# A2 — frame-builder (parametric engine + its two palettes)

**A1 review note:** advisor accepted A1 as written; the AddIns-folder-clean question (A1-6) was
answered — the advisor deleted the two files there by hand after E7a, so the overlay-gap risk
(A1-6) is real and still unguarded, not a false alarm. Carrying that forward, not re-litigating it.

**Scope read this turn:** `fb_engine/` full read of `parametric_engine.py` (484 L), `build_context.py`
(272 L, targeted), `frame_engine.py` (targeted: `FrameBuilder` class, `_create_skeletal_parameters`,
the two `run_*` entry points); `ui/sketch_builder_ui.py` (594 L) + `ui/solid_builder_ui.py` (351 L)
full read + diffed against each other; the 3 root-level test files (1013 L); `UNDO-REDO-DESIGN.md`
(E8's own investigation doc) read in full as ground truth for the param-lifecycle question.

## A2 findings

| ID | area | file:line | class | symptom | evidence | fix shape | severity |
|---|---|---|---|---|---|---|---|
| A2-1 | fb_engine param lifecycle | `frame_engine.py:225-320` (`_create_skeletal_parameters`) + `parametric_engine.py:246-289` (`_sync_user_parameters`) | bug / honesty | **Directly answers the dispatch's question — yes, other params are still created inside Execute, and it contradicts E8's own root-cause doc.** Both functions call `userParameters.add(...)` for any missing template/UI parameter (width, height, thickness, etc.), and BOTH run inside the same `HiddenBuildCommandExecuteHandler.notify` → `_run_sketch_build_direct` → `run_sketch_only`/`run_full_synthesis` → `build_template` chain that `ensure_tilt_param`'s own docstring (`parametric_engine.py:47-56`) says a param-create must stay OUTSIDE of ("a mid-build user-parameter create doesn't reverse with the geometry and orphans on Ctrl+Z"). `UNDO-REDO-DESIGN.md` §3 (the E8 investigation itself) concludes `frame_tilt_deg` was **"the first design-level user parameter created mid-build"** and scopes fixes F1-A/B/C to that one parameter — but `_create_skeletal_parameters` is in `frame_engine.py`, a file the tilt commit `6c1cce4` **never touched** (per the same doc's own line 32: "touched only `parametric_engine.py`"), and it already creates Master + Dependent template params the identical way. On a genuinely fresh document's first build, Ctrl+Z would plausibly orphan MULTIPLE parameters (width/height/thickness/…), not just `frame_tilt_deg` — meaning the diagnosis in `UNDO-REDO-DESIGN.md` looked only at the commit diff, not at the pre-existing code it called into, and may have scoped the fix too narrowly. | Traced the full call chain: `sketch_builder_ui.py:396` (`HiddenBuildCommandExecuteHandler`) → `:409` → `:464` (`_run_sketch_build_direct`) → `frame_engine.py:52` (`build_sketch_logic_v3`) → `:149`/`:174` (`run_sketch_only`/`run_full_synthesis`) → `:157`/`:182` (`_create_skeletal_parameters`, BEFORE `build_template`) → also `:165`/`:190` `build_template` → `parametric_engine.py:198` (`_sync_user_parameters`, inside `build_sketch`). `grep` confirms `frame_engine.py`'s `_create_skeletal_parameters` (lines 225-320) predates/is untouched by the isolated tilt-only diagnosis. | either (a) re-run the UNDO-REDO-DESIGN.md repro (fresh doc, first build, Ctrl+Z, check Parameters dialog for width/height/thickness, not just tilt) to confirm/refute at runtime, or (b) apply F1-C's pattern (ensure ALL template master/dependent params exist at palette-open, mirroring `ensure_tilt_param`) rather than F1-B's "accept a lingering param" scoped to tilt alone | **H** (UNVERIFIED at runtime — this lane has no Fusion access — but the code-level contradiction with the design doc's own stated scope is solid static evidence, not speculation) |
| A2-2 | fb_engine param lifecycle | `build_context.py:251-272` (`create_or_update_param`) | dead | Zero callers anywhere in the repo (`grep -rn "create_or_update_param"` → only the `def` line). This is the exact function `UNDO-REDO-DESIGN.md:20-21` cites as part of the OLD (pre-E8) tilt-plane chain (`_get_tilt_plane` → `create_or_update_param('frame_tilt_deg', ...)` → `userParameters.add`) — current `_get_tilt_plane` (`parametric_engine.py:384-439`, read in full) no longer calls it; E8 replaced that call with a read-only `itemByName` check + the palette-open `ensure_tilt_param`. Orphaned by the same migration this audit is cross-checking. | `grep -rn "create_or_update_param" --include=*.py .` → 1 hit (the definition). Read `_get_tilt_plane` in full — confirmed no call site. | delete | L |
| A2-3 | fb_engine phase pipeline | `parametric_engine.py:291-346` (`_build_blocks`) | bug | Confirms the dispatch's exact concern (**"a sketch left deferred = silent wrong geometry"**) is a real, un-mitigated gap, not just a theoretical one. `_build_blocks`'s `isComputeDeferred` True/False windows are correctly balanced on the HAPPY path (every block ends `False` at `:346`, so the loop and the method both leave the sketch live) — but there is **no `try`/`finally`** around the block loop or around `_process_sequence`/`step_step`/`miter_step`. The ONLY exception handling in the call chain is one layer up, in `build_template`'s per-sketch `try/except` (`parametric_engine.py:157-179`), which logs the crash and moves on but **never touches `sketch.isComputeDeferred`**. So a step that raises WHILE deferred is `True` (any point between `:313`-`:330` or `:334`-`:346`, or inside `_process_sequence`) leaves that Fusion sketch object permanently in deferred-compute mode for the rest of the session — exactly the silent-wrong-geometry failure mode named in the dispatch. | Read `_build_blocks` (`:291-346`) + `_process_sequence` (`:348-`) + the per-sketch crash handler (`:157-179`) side by side; confirmed no `isComputeDeferred = False` anywhere in the crash path. | wrap the block body (or at minimum the two deferred windows) in `try/finally: sketch.isComputeDeferred = False` so a crash always leaves the sketch live/solved, even if the geometry is incomplete | M-H (UNVERIFIED at runtime — needs a forced-failure repro to confirm the geometry actually reads wrong, not just the flag staying `True`) |
| A2-4 | ui/ | `ui/sketch_builder_ui.py` + `ui/solid_builder_ui.py`, whole-file structural | principle #4 (declare-over-hand-roll) | The hidden-command dispatch machinery (`_create_hidden_command`, `_ensure_hidden_commands`, `_schedule_hidden_build`, the `HiddenBuild*CommandCreatedHandler`/`ExecuteHandler` pair, the status/notify helpers) is hand-rolled TWICE in near-parallel structure — `diff` shows 555 changed lines out of 945 total across the two files, almost entirely differing only in command IDs/names/the specific build call, not the mechanism. Same duplication class STANDARDS-AUDIT §1a already flagged for the editor tree (B8) — not previously flagged for this pair. | `diff ui/sketch_builder_ui.py ui/solid_builder_ui.py \| wc -l` → 555; read both files in full, confirmed the dispatch scaffolding is structurally identical apart from names/IDs. | extract the hidden-command dispatch pattern into one shared helper (e.g. `fb_utils/hidden_command.py`) parameterized by cmd id/name/handler/build-fn, both UIs call it | M (structural debt, not a live bug) |
| A2-5 | fb_engine param lifecycle | `parametric_engine.py:123-125` | honesty | `build_template()`'s comment — "We no longer handle parameter creation here to avoid overwriting parametric expressions with raw numeric factors" — is misleading, not false: `build_template()` itself indeed doesn't create params directly, but the comment implies creation is now centralized in ONE place (`frame_engine.py`) when in fact THIS SAME FILE's `_sync_user_parameters()` (a few dozen lines later, reached via `build_sketch()` in the same call chain — see A2-1) also creates params. Two creation sites, not the one the comment implies. | Read `:108-126` (the comment) against `:246-289` (`_sync_user_parameters`) in the same file. | reword to name both sites, or fold A2-1's fix in and make the comment true by actually centralizing | L |

**Inefficiencies:** none rising to a finding — `_process_sequence`'s `"Pulse"` step (`:366-369`) toggles
`isComputeDeferred` False→True as an explicit manual re-solve; used sparingly per grep, not a hot loop.

## What's GOOD

- **UI lifecycle symmetry confirmed, extends A1's verdict.** `sketch_builder_ui.py`'s `run_palette`
  (`:525-569`) registers a `documentActivated` handler (`:568-569`) that `solid_builder_ui.py` correctly
  does NOT have (solid has no doc-reactive behavior) — this exactly matches what A1 already verified the
  parent's `_teardown_submodules` releases (sketch: removes the `documentActivated` sub + clears
  `handlers`; solid: just clears `handlers`). No drift between what each UI registers and what the parent
  releases.
- **F2 (delete the dead undo-transaction wrappers) is DONE.** `UNDO-REDO-DESIGN.md` recommended deleting
  `_start_undo_transaction`/`_commit`/`_abort` from both builder UIs (dead — `app.startTransaction`
  never exists). `grep -n "_start_undo_transaction\|startTransaction\|_commit\|_abort"` over both UI
  files → 0 hits. Already actioned, correctly.
- **`_get_tilt_plane` itself (the post-E8 code) is clean** — read-only on the parameter, reuse-by-name
  for the plane, graceful multi-level fallback to the raw XY plane, accurate docstring citing
  `UNDO-REDO-DESIGN.md` by name. No findings on this function itself; it's the pattern everything else
  in A2-1 should have followed.

## Honesty sweep (beyond A2-5 above)

Checked the specific claims NEXT-SESSION named: E8's undo-transaction-wrapper removal — confirmed true
(see "What's GOOD"). C4's shared-module move — out of this folder's scope (fb_shared, not frame-builder).
No other stale "no longer true" claims found in `fb_engine/*.py` docstrings beyond A2-5; the several
"used to X, now Y" comments in `appearance_strategy.py:7`, `document_discovery.py:8`,
`parameter_schema.py:7`, `template_resolver.py:7` are all accurate past-tense refactor notes (verified
each describes the CURRENT shape correctly), not stale claims — the honest kind, not the A2-5 kind.

## Test gap

**Confirmed concretely, not just "light coverage":** the 3 test files at `frame-builder/` root
(`test_templates.py` 193L, `test_appearance_strategy.py` 276L, `test_document_discovery.py` 544L —
1013L total) — `grep -l` for `parametric_engine|_sync_user_parameters|_create_skeletal_parameters|isComputeDeferred|sketch_builder_ui|solid_builder_ui` across all three → **0 matches**. Every file this
turn's findings live in (the entire parameter-lifecycle mechanism in A2-1/A2-2/A2-5, the phase-pipeline
crash gap in A2-3, both UI files in A2-4) has **zero** test coverage. This sharpens
STANDARDS-AUDIT §2's "frame-builder `fb_engine` — light" and "Python add-in lifecycle — none" into a
concrete claim: the exact code this turn found a plausible undo-correctness bug in (A2-1) and a
silent-wrong-geometry gap in (A2-3) is precisely the UNTESTED part of an already-lightly-tested folder.

## What I could not verify (A2)

- A2-1 and A2-3 are both static-only findings — this lane has no `fusion_execute` access (explicitly
  off-limits per NEXT-SESSION). Both need a live-Fusion repro to move from "code says this can happen"
  to "confirmed happens": A2-1 needs the `UNDO-REDO-DESIGN.md` §5 repro re-run while ALSO watching
  width/height/thickness (not just `frame_tilt_deg`) in the Parameters dialog after Ctrl+Z on a fresh
  document's first build; A2-3 needs a forced mid-block failure (e.g., a malformed template) to confirm
  the resulting geometry actually reads wrong, not just that the flag stays `True`.
- `fb_engine/offsets.py`'s own `isComputeDeferred` window (`:64-78`, flagged in its own comment as
  intentional) — read the surrounding comment but did not fully trace whether IT has the same
  no-reset-on-exception gap as A2-3; flagging as unchecked rather than assuming it's fine.
- `sketches/template_1/` and `sketches/template_2/` phase modules (the actual template DNA / geometry
  step definitions) — not read this turn; A2's scope was the engine + UI shell, not the per-template
  phase content.

---

# A3 — template-maker

**A2 review note:** advisor accepted A2. A2-1 (params created inside Execute beyond the tilt) is being
verified live by the advisor directly; A2-3 (deferred-compute window, no `finally`) goes to seat A as a
declared context manager, folding in A2-2/A2-5; A2-4 (duplicated hidden-command machinery) is queued
design-first. Carrying nothing forward to re-verify this turn — those are the advisor's/seat A's threads.

**Scope read this turn:** `template-maker.py` (860 L, full — entry, `run()`/`stop()`, `_PROJECT_MODULES`
+ `_reload_all_project_modules`); `core/check_addin_sync.py` (29 L, full); targeted reads of
`coincidence_clusters.py`, `relation_hints.py`, `phase_parser.py` for loop/COM patterns; grepped all of
`core/` for fb_shared duplication (none found) and cross-module import graphs; `ui/template_maker_palette.html`
(1663 L, targeted: every `fusionSendData`/`sendInfoToHTML` call site) against `template-maker.py`'s
dispatcher; ran `pytest template-maker/tests -q` (read-only, 83 passed) and cross-referenced which core
modules each test file actually imports.

## A3 findings

| ID | area | file:line | class | symptom | evidence | fix shape | severity |
|---|---|---|---|---|---|---|---|
| A3-1 | template-maker.py entry | `template-maker.py:82-100` (`_PROJECT_MODULES`) | bug / P2-violation | **Answers the dispatch's question directly — the reload machinery is still needed, and it's incomplete.** `_PROJECT_MODULES` (the force-wipe-on-`run()` list) lists 17 names but `core/` has 23 importable modules; **5 are missing**: `detection_log`, `dimension_hint`, `offset_hint`, `template_bridge`, `variable_scan`. This is the exact B7 mechanism (a bare-name module cached in `sys.modules` survives Stop→Start because nothing wipes it, so a fresh reload of a module THAT'S in the list still re-binds to the STALE cached copy of one that ISN'T) — but with a much wider blast radius than B7's `selection_items.py` ever had: `detection_log` alone is imported by 9+ other core modules (`cc_proxy`, `coincidence_clusters`, `detect_projections`, `dimension_hint`, `fb_attributes`, `offset_hint`, `ownership_gate`, `relation_hints`, `rename_selection`, `template_bridge`, `template_payload`, `template_payload_builder`, `variable_scan`, plus `template-maker.py` itself). Also not covered by the PARENT's `_shared_project_names` wipe list (`bspline-frame-builder.py:251-262`, checked in A1) — these 5 names appear nowhere in either wipe list. Net effect: editing `detection_log.py` (or the other 4) and hitting Stop→Start does NOT pick up the change — needs a full Fusion restart, violating P2 exactly the way B7 did. | `_PROJECT_MODULES` has 17 entries; `core/` has 23 `.py` files (excluding `check_addin_sync.py`, itself not meant to be imported as a project module — see A3-2). Diffed the two lists by hand. `grep -rn "from detection_log import\|from dimension_hint import\|from offset_hint import\|from template_bridge import\|from variable_scan import"` across `core/` and `template-maker.py` → 14 import sites across 10 files for these 5 names combined. Not previously tracked (`grep` on `BUGS_OPEN.md`/`STANDARDS-AUDIT.md` → 0 hits for any of the 5 names). | add all 5 to `_PROJECT_MODULES` (mirrors the exact B7/E7a fix pattern already applied to frame-inspector) | **H** (same class as B5/B7, wider blast radius; UNVERIFIED at runtime — no Fusion access this lane, but the missing-name diff itself is unambiguous static evidence) |
| A3-2 | template-maker/core | `core/check_addin_sync.py` (29 L, whole file) | dead / honesty | **Answers "is it live or dead?"** — technically runnable (a standalone script, zero importers anywhere in the repo — confirmed by grep) but its PREMISE is stale: it diffs the workspace against `%APPDATA%\...\AddIns\template-maker\` (`:6-10`), a STANDALONE per-palette AddIns folder. Per A1, template-maker installs as part of the UNIFIED `bspline-frame-builder` add-in now (`DEPLOY_bspline-frame-builder.py`'s `deploy_local`) — the standalone folder this script targets only gets populated by the legacy `deploy_template_maker()` path (A1-4, itself already found to have stale `verify_files`). Separately, even if the premise were current, the scan itself is shallow: `os.listdir(WORKSPACE_DIR)` + `os.path.isfile` (`:12`) is NOT recursive — it only ever compares template-maker's ROOT-level files, never `core/` or `ui/`, so it would silently miss drift in the very files (`core/*.py`) that matter most. | Read the file in full (29 L). `grep -rn "check_addin_sync" --include=*.py .` (from `template-maker/`) → 0 hits outside its own definition. | either delete (if the unified deploy makes it obsolete) or rewrite against the unified `AddIns/bspline-frame-builder/template-maker/` path + `os.walk` for a recursive check | L |
| A3-3 | template-maker/core | `core/coincidence_clusters.py:424-445` (`_auto_pair_...` loop) | inefficiency | An O(points × curves) nested loop, each pair calling `_point_on_curve_distance(point, curve)` (a Fusion geometry/COM call). Matches the dispatch's "N² loops over entities" concern in SHAPE, but bounded by `entities` — the user's current SELECTION (per the function's own docstring: "points in selection pick order... curves in selection pick order"), not a full sketch's entity count. Real sketches can have hundreds of entities, but a user's selection for a coincidence-hint pass is typically much smaller — flagging as a real but likely-LOW-impact inefficiency rather than a confirmed hot path, since I didn't find a caller that feeds this the WHOLE sketch rather than a selection. | Read `:391-445` in full; traced the docstring's "selection pick order" framing back to the caller pattern (a user-driven multi-select action, not a full-sketch walk). | if a caller is ever found that passes full-sketch entities (not just selection), pre-bucket by proximity (e.g. spatial hash) before the O(n·m) pass; otherwise leave — the bound is small in practice | L (UNVERIFIED — did not trace every caller to confirm the selection-only bound holds everywhere) |

**Duplication with fb_shared:** none found. `grep` for local re-definitions of `get_fb_name`/`get_fb_metadata`/
`_get_arc_midpoint`/`format_point`/`get_entity_coord` (the fb_shared function names) anywhere in `core/`
→ 0 hits — confirms A1's fb_shared-consolidation finding holds for A3's scope too; no shadow
reimplementation under different names either.

## What's GOOD

- **`template-maker.py`'s own `run()`/`stop()` lifecycle symmetry is clean and is the pattern B5 already
  cites as correct.** `run()` (`:647-753`) defensively removes any previous `_sel_handler`/
  `_doc_activated_handler` before re-adding (`:734-751`, self-healing even across an unclean prior stop);
  `stop()` (`:759-`) symmetrically removes both (`:817-835`) PLUS tears down the `deferred_rebuild`
  CustomEvent separately from the session-scoped refresh event (`:837-847`, correctly documented as
  intentional). `_handlers.clear()` runs in `finally` (`:855-857`), same defensive pattern as the parent
  and frame-inspector. This is exactly the reference implementation `BUGS_OPEN.md` B5 points to
  ("Contrast the correct pattern: template-maker detaches sel+doc handlers in stop()") — re-verified
  directly rather than taking that citation on faith.
- **Palette doorless-handler sweep: clean, both directions.** JS→Python: every `adsk.fusionSendData(...)`
  action string (`'copy'`, `'clusterPicks'`, `'poll'`, `'settings'`, `'rename'`) has a matching
  `elif action == '...'` branch in `template-maker.py`'s dispatcher (`:487-569`) — confirmed each by
  name, not just counted. Python→JS: both `sendInfoToHTML` event names (`'update'`, `'build_info'`) have
  a matching `if (action === '...')` handler in the palette HTML (`:1257`, `:1278`). No dead sends, no
  doors without rooms.

## Test gap

**83/83 pass** (`pytest template-maker/tests -q`, read-only, 1.38s). Cross-referenced which `core/`
modules each test file imports: `phase_parser`, `rename_selection`, `offset_hint`, `dimension_hint`,
`coincidence_clusters`, `ownership_gate`, `relation_hints`, `template_bridge`, `template_generator`,
`template_naming`, `template_payload`, `fb_shared.entity_helpers`/`.expression_coords` are all directly
exercised. **9 of 23 `core/` modules have ZERO references anywhere in `tests/`** (not just "no dedicated
test file" — grepped each name across every test file): `entity_util`, `cc_proxy`, `role_points`,
`deferred_rebuild`, `template_code`, `variable_scan`, `detect_projections`, `detection_log`,
`check_addin_sync`. Two more (`fb_attributes`, `template_payload_builder`) have exactly 1 reference each
(not independently verified as a dedicated test vs. an incidental import). **Sharper point than the raw
count:** `detection_log` — the module at the CENTER of A3-1's finding — has zero test references, AND
more fundamentally, **the pytest suite structurally cannot exercise A3-1 at all**, dedicated tests or
not: it's a Stop→Start `sys.modules`-caching bug specific to Fusion's long-running process +
`_force_wipe`/`importlib.reload` semantics; each `pytest` run is a fresh Python process with fresh
imports, so there's no reload cycle for the bug to manifest in. A3-1 needs a live-Fusion Stop→Start
repro (edit `detection_log.py`, Stop→Start, confirm the edit is/isn't picked up), not a unit test.

## What I could not verify (A3)

- A3-1: static-only, no Fusion access this lane (explicitly off-limits per NEXT-SESSION beyond the
  read-only `pytest` run). The missing-name diff is solid static evidence; the actual stale-reload
  SYMPTOM needs a live Stop→Start repro to confirm.
- A3-3: did not trace every caller of the `coincidence_clusters.py` pairing function to confirm the
  "bounded by selection, not full sketch" claim holds everywhere — flagged as the specific unverified gap.
- `core/phase_parser.py` (625 L) and `core/relation_hints.py` (553 L) — read for loop-shape/COM-call
  patterns only (per A3-3's search), not read end-to-end for correctness the way A1/A2's named files
  were; a deeper pass on these two specifically could surface more than this turn's scope allowed.
- `ui/template_maker_palette.html`'s 1663 lines — only the ~15 `fusionSendData`/`sendInfoToHTML` call
  sites were read for the doorless-handler check; the rest of the rendering/declared-vs-hand-rolled
  question (explicit A3 scope item 3) was not otherwise audited this turn.

---

# A4 — stamp-editor

**A3 review note:** advisor accepted A3. A3-1 becomes a seat-A task, but reframed: not "add the 5
missing names" but DERIVE `_PROJECT_MODULES` from `core/`'s actual file list so a 6th omission can't
recur (the hand-maintained list IS the bug, not just its current contents). A3-2 (`check_addin_sync.py`)
→ delete. Nothing to re-verify this turn.

**⚠ Worktree-specific limitation, disclosed up front:** `stamp-editor/html/editor/` and
`stamp-editor/html/core/stamp/` (the GENERATED copies per C1) do not exist ON DISK in this `lane-b`
worktree at all — confirmed (`find html -maxdepth 2 -type d` → only `core/`, `main/`, `styles/`; the on-disk
`core/` holds only the 2 unique files `engine.js`/`runtime.js`). This is expected: lane-b is a fresh git
worktree checkout, and the generated files are gitignored (correctly, per C1) — they only exist on disk
in a tree where someone has actually RUN `sync_stamp_bundle.py`, which the main checkout has but this
worktree never has. **This means the "has any generated copy been hand-edited since its source?" question
cannot be answered from this lane** — there's nothing here to diff against. Answered everything else
(tracked-in-git status, the writers/readers map, lifecycle, doorless handlers, dead code, tests) from
what IS available: git's index, the 8 unique tracked files, and `b-spline-gen/html/` (the sync source).

## Fork status (item 1)

**Tracked-in-git: clean, matches C1 exactly, no re-drift.** `git ls-files bspline-frame-builder/stamp-editor`
returns exactly the 8 UNIQUE files C1 (turn 53) classified as hand-written and kept tracked —
`html/core/engine.js`, `html/core/runtime.js`, `html/index.html`, `html/main/{editor-bridge,layers,main,
ui-bindings}.js`, `html/styles/stamp-editor.css` — plus `ressources/*.png`, `stamp-editor.manifest`,
`stamp-editor.py`, `workspace_link.json`. **None of the 54 generated files (`editor/**`, `core/stamp/**`,
the 4 core deps) are tracked** — confirms the de-fork held. **Hand-edited-since-source: UNVERIFIABLE from
this lane** (see the disclosure above) — needs re-checking from a tree where sync has actually run
(the main checkout, or a fresh `sync_stamp_bundle.py` run, which is explicitly off-limits here).

## A4 findings

| ID | area | file:line | class | symptom | evidence | fix shape | severity |
|---|---|---|---|---|---|---|---|
| A4-1 | stamp-editor.py | `stamp-editor.py:157-158` (`reset_ui` handler) | dead | Python dispatcher branch for action `'reset_ui'` has ZERO JS callers anywhere under `html/` — the reverse of B4 (a "room" with no "door"). Traces back to the file's own header comment (`:13-15`): `"v1 SCAFFOLD: this file wires the toolbar button + palette + the basic log/ping/reset_ui round-trip. Face-pick capture and STEP emission land in subsequent passes."` — that scaffold-era round-trip predates the file's growth to 1436 lines with face-pick/commit/preview_mesh/preview_clear/request_face_grid all now live; `reset_ui` is the one scaffold action the JS side never kept (or never added). | `grep -rn "reset_ui" html/ stamp-editor.py` → 2 hits, both in `stamp-editor.py` (the `elif`-equivalent branch + its log line); 0 in `html/`. | delete the branch, or wire a JS caller if a UI reset affordance was actually intended | L |
| A4-2 | stamp-editor.py | `stamp-editor.py:13-15` (file header) | honesty | The header still reads `"v1 SCAFFOLD"` and frames face-pick + STEP emission as future work ("land in subsequent passes") — both are fully implemented today (`pick_face`/`request_face_grid` handlers, `commit` for STEP emission, all confirmed live in the dispatcher). A reader opening this file cold would believe it's an early stub, not the 1436-line file it actually is. | Read `:1-25` (header) against the full dispatcher list (`:138-210`, 7 live actions) and the lifecycle functions (`run`/`stop`, `:1254-1436`). | reword the header to describe the current shape, or delete the stale "v1 SCAFFOLD" framing | L |
| A4-3 | stamp-editor.py / runtime.js | `html/core/runtime.js:1-7` (module docstring) | honesty (UNVERIFIED) | Docstring claims runtime.js "Mirrors step-editor's runtime, kept identical so the two add-ins share the same wire shape." No `step-editor/` Fusion add-in exists anywhere in the repo (only `cloud/step-editor-pages` + `cloud/step-editor-worker`, and per STANDARDS-AUDIT §3 `step-editor-pages` is itself "README-only; the `deploy.cmd` + `step-editor/html/` source it references don't exist"). Could be a forward-looking/aspirational note (written for when step-editor gets built) rather than a false claim about a currently-mirrored sibling — genuinely ambiguous from static text alone, flagging rather than asserting either way. | `find . -maxdepth 2 -iname "step-editor*"` (repo root) → only `cloud/step-editor-pages`, `cloud/step-editor-worker`; no Fusion add-in folder. | if aspirational, say so explicitly ("designed to match step-editor's eventual runtime"); if it once mirrored a real file that's since been deleted, drop the claim | L (UNVERIFIED — could not confirm intent, only that no current sibling exists to mirror) |

**Nothing else dead-code/hand-rolled beyond A4-1 found in the unique JS** (`main/{editor-bridge,layers,
main,ui-bindings}.js`, `core/{engine,runtime}.js`) this turn — read `runtime.js` in full (39 L, clean,
well-declared `isFusion`/`sendToPython`/`pyLog` wrapper trio); `main/*.js` and `core/engine.js` were
grepped for the doorless-handler question (below) and dispatch patterns but not read end-to-end for
every hand-rolled-table candidate — see "what I could not verify."

## Doorless handlers (item 3) — one gap found (A4-1), otherwise clean

**JS→Python:** 7 actions sent via `sendToPython(...)` in `main/*.js` (`cancel`, `commit`, `pick_face`,
`ping`, `preview_clear`, `preview_mesh`, `request_face_grid`) plus `'log'` from `runtime.js`'s `pyLog` —
all 8 have a matching branch in `stamp-editor.py`'s dispatcher (`:141-210`). **Python→JS direction not
exhaustively cross-checked this turn** (see gaps below) beyond confirming `_send_to_palette('face_count_update', ...)`
(the live-face-count feature) has a live JS consumer, spot-checked in `main.js`.
**Reverse gap:** `reset_ui` (A4-1) — a Python door with no JS room.

## EDM4/C5 reconnaissance — P.stampLayers writers/readers map (item 4, read-only)

Scoped to what's findable given the worktree limitation: **no `stampLayers` references exist in
stamp-editor's own on-disk files** (`grep -rn stampLayers html/` from `stamp-editor/` → 0 hits) — the
concept lives entirely in `b-spline-gen/html/` (the sync SOURCE; stamp-editor's generated `editor/`
mirror almost certainly duplicates `editor/layers.js`'s references 1:1 per C1's no-rewrite sync, but
that mirror is absent from this worktree to confirm directly — flagging, not asserting). Mapped across
`b-spline-gen/html/` (13 files matched `grep -rl stampLayers`):

**WRITERS** (assignment into `P.stampLayers` or a `[idx]` field):
- `core/state.js:106` — the shape's origin declaration (`stampLayers: [...]` in the initial `P` object).
- `core/state.js:211-212,215,220-221,226-227` — per-layer `svg`/`enabled`/`mask` setters.
- `core/state.js:253-254` — `P_physical.stampLayers = P_physical.stampLayers.map(...)` — the B6
  hidden-layer-mask-strip mechanism (BUGS_OPEN B6's "destructive save" site).
- `core/state.js:368-369` — generic `layerSpecific[key]` setter onto the active layer.
- `main/app-init.js:117,121-122` — SVG-editor-close writeback (`P.stampLayers[idx].svg/.mask = ...`).
- `main/snapshot-manager.js:41-42` — snapshot-restore writeback to layer 0.
- `main/stamp-mask-manager.js:121` — `setStampLayerMask(idx, result)` (mask-compute writeback; the
  setter itself not traced this turn — flagging as an indirect writer, not a confirmed line-level one).

**READERS** (access without assignment):
- `core/engine/rebuild.js:214,236-237` — rebuild-time fallback read (legacy-vs-editor-roster merge).
- `core/history.js:33` — undo/redo snapshot clone (`JSON.parse(JSON.stringify(P.stampLayers || []))`).
- `editor/layers.js:7,30,35` (comments only — the b-spline-gen EDITOR's own layer roster has already
  migrated OFF `P.stampLayers` onto `editor._layers`; these 3 lines document that past migration and a
  still-live "keep both sides in sync" obligation, not a live read/write site themselves).
- `main/app-init.js:29,54` — existence/presence checks (`.find`, `.some`).
- `main/cloud-preset-manager.js:23-24`, `main/cloud-project-manager.js:64-65,669`,
  `main/preset-manager.js:54-55` — read + clone for cloud/local save (all three strip `.mask` before
  persisting, the same pattern, hand-rolled 3×  — itself a P4 candidate, noted but not promoted to a
  finding since it's outside A4's stamp-editor scope).
- `main/export-flow.js:41-42,256` — STEP-export filtering (`.filter(isCarvingLayer)`, etc.).
- `main/snapshot-manager.js:55` — presence check for the snapshot UI.
- `main/stamp/layer.js:44,51,128` — fallback read when the editor's own layer roster isn't loaded yet.
- `main/stamp/_shared.js:39,44-45` — active-layer lookup + fillet-radius presence check.
- `main/stamp-mask-manager.js:61-62,66,81,83` — legacy-fallback iteration + tooling resolution.

**Not a carve proposal** — per the dispatch, this is reconnaissance only. The shape that jumps out for
whoever does the C5 design: `core/state.js` is the only WRITER of the raw array; every other write site
writes into a specific layer's field, never reshapes the array itself except the B6 mask-strip at `:253-254`.

## Test gap (item 5)

**Zero of stamp-editor's 8 unique files are imported by any of the 4 vitest files at repo-root `tests/`.**
Checked directly (`grep -rhoE "^import .* from '[^']+'" tests/*.js`) — every import resolves to
`b-spline-gen/html/...` paths (`core/state.js`, `editor/editor-coords.js`, `main/stamp/_shared.js`,
`main/app-init.js`, `main/stamp/svg-source.js`). None resolve to `stamp-editor/html/...`. Since the
GENERATED portion of stamp-editor (`editor/`, `core/stamp/`) is a byte-identical mirror of tested
b-spline-gen source (per C1), it gets INDIRECT coverage-by-proxy — a bug in the shared logic would be
caught via the b-spline-gen-side test. **But the 8 UNIQUE files have zero coverage, direct or indirect**
— nothing exercises `main/main.js`, `main/layers.js`, `main/editor-bridge.js`, `main/ui-bindings.js`,
`core/engine.js`, or `core/runtime.js`. Could not actually EXECUTE `npx vitest run` in this worktree
(`node_modules` isn't installed here — a separate worktree, never `npm install`'d) to confirm the
"29 green" count independently; relied on the static import-graph instead, which is sufficient to answer
"which touch stamp-editor code" without needing to run anything.

## What's GOOD

- **`_enable_live_face_count`/`_disable_live_face_count` (`stamp-editor.py:243-275`) is exemplary
  lifecycle code — read in full, not just cited.** Idempotent-guarded (`if _active_sel_handler is not
  None: return`), removes from BOTH `ui.activeSelectionChanged` AND the module `handlers` list, and is
  called from BOTH `PaletteClosedHandler.notify` (`:231`, palette closed without a full Stop) AND
  `stop()` (`:1400`, full add-in Stop) — covering both the ways this subscription could otherwise
  survive. This re-confirms, with the actual code read rather than the citation trusted, what B5 already
  names as the correct pattern frame-inspector should follow.
- **`run()`/`stop()` toolbar-button symmetry is clean** (`:1254-1436`, both read in full): `run()`
  defensively cleans a stale palette + old command def before creating fresh ones; `stop()` mirrors it
  exactly (palette deleteMe, per-panel control removal, command-def removal, `handlers.clear()`). The
  icon-cache-busting `ressources_<mtime>` folder trick (`:1309-1346`) self-sweeps older copies on the
  NEXT `run()` rather than requiring `stop()` cleanup — a deliberate, documented design choice, not a
  leak.
- **`runtime.js` itself (the unique bridge module) is clean** — 39 lines, three small declared functions
  (`isFusion`, `sendToPython`, `pyLog`), no dead code, no hand-rolled duplication of the pattern found
  elsewhere (contrast A2-4's two-builder-UI duplication — this file doesn't repeat that mistake).

## What I could not verify (A4)

- **Hand-edited-drift on the generated copies** — cannot be checked from this worktree at all (see the
  disclosure at the top of this section). Needs a run from the main checkout, or `sync_stamp_bundle.py`
  run here then diffed against git-stash/backup (both off-limits per this turn's "do not run it"
  instruction) — flagging for the advisor to check from a tree where the generated files actually exist.
- **Full doorless-handler sweep, Python→JS direction** — only spot-checked `face_count_update`; did not
  enumerate every `_send_to_palette(...)`/`sendInfoToHTML(...)` call site in `stamp-editor.py` against
  every JS-side event listener the way A1/A3 did for their palettes. Given the file's size (1436 L) and
  the time already spent on A4-1's discovery, this is the one explicit scope item not run to the same
  depth as A1/A3's doorless sweeps.
- **`main/main.js`, `main/layers.js`, `main/editor-bridge.js`, `main/ui-bindings.js`** — grepped for
  dispatch patterns and stampLayers, not read end-to-end for hand-rolled-table candidates the way
  runtime.js was.
- `setStampLayerMask` (cited as a stampLayers writer above) — the function's OWN definition/line number
  wasn't traced; only its call site at `stamp-mask-manager.js:121` was read.
- The vitest suite's actual "29 green" count — relied on the dispatch's own stated number + the static
  import graph, did not execute the suite (no `node_modules` in this worktree).

---

# A5a — b-spline-gen core + main + b-spline-gen.py

**A4 review note:** advisor accepted A4; the generated-copy drift check was done from the main checkout
(recorded in ROADMAP). Nothing to re-verify this turn.

**Scope read this turn:** targeted greps across all of `core/` (24 files, 8790 L) + `main/` (26 files,
4146 L) for the P1/dead-code/export questions; full reads of `core/history.js` (93 L), `core/engine/
scheduler.js` (23 L), `main/snapshot-manager.js` (72 L), the 3 `buildSnapshot`-shaped functions
(`cloud-preset-manager.js`, `cloud-project-manager.js`, `preset-manager.js`), `core/state.js`'s
`setStampLayer*`/`saveLastSession` region (`:210-260`); `b-spline-gen.py`'s `run()`/`stop()`
(`:1508-1634`) plus a full-file grep for handler registration/clearing. Node modules still absent in
this worktree (known gap, did not `npm install`) — test-coverage answered via the static import graph.

## A5a findings

| ID | area | file:line | class | symptom | evidence | fix shape | severity |
|---|---|---|---|---|---|---|---|
| A5a-1 | core/history.js | `core/history.js:24-41` (`takeSnapshot`) | bug / P4-violation | Every OTHER place that persists `P` (`core/state.js:238-260` `saveLastSession`, and all 3 `buildSnapshot` variants below) explicitly strips `.stampLayers[*].mask` FIRST because `JSON.stringify` turns a `Float32Array` into `{"0":v,"1":v,...}` — a plain object, "bloating localStorage by ~10× and producing an unusable shape on reload" (the codebase's own words, `state.js:249-251`). `takeSnapshot` — the UNDO/REDO snapshot function, called on every undo-worthy action — is the ONE exception: `JSON.parse(JSON.stringify(P))` at `:28` round-trips the mask through the same corruption with no strip first. In practice this is USUALLY masked: `applySnapshot` (`main/snapshot-manager.js:41-58`) unconditionally regenerates masks via `updateStampMasks()` whenever `hasStampSvg` is true, overwriting whatever corrupted shape `takeSnapshot` produced. But when `hasStampSvg` is FALSE at the moment of undo/redo (the layer being restored to has no active svg), the regen is skipped (`:56` gate) and the corrupted `{"0":...}` object survives in `P.stampLayers[i].mask` — `rebuild()` "silently skips any layer whose `.mask` is null or whose body channel is missing/wrong length" (comment at `snapshot-manager.js:50-51`, describing its OWN downstream effect), so a stamp could silently fail to imprint post-undo in that narrow window. Also a real, avoidable perf cost: every `takeSnapshot()` call JSON-serializes a full grid-resolution Float32Array it's going to discard the value of anyway. | Read `takeSnapshot` (`:24-41`) side-by-side with `saveLastSession` (`state.js:238-260`) and `applySnapshot` (`snapshot-manager.js:10-71`, especially the mask-regen comment at `:46-52` which documents the STRIP-then-REGEN contract every other site honors). | add the same one-line strip (`stampLayers: (P.stampLayers\|\|[]).map(L=>({...L,mask:null}))`) to `takeSnapshot`'s `P` clone — ideally via the SAME declared serializer A5a-2 below recommends, so this can't drift again | M-H (the corruption is certain and reproducible from the code; the DOWNSTREAM silent-skip is narrower — UNVERIFIED at runtime whether `hasStampSvg`-false-at-undo is a realistic sequence a user hits, but the perf cost and the shape-corruption itself are unconditional facts, not conditional) |
| A5a-2 | main/ | `main/preset-manager.js` (121 L) + `main/cloud-preset-manager.js` (162 L), whole files | dead | **Reframes item 2's premise.** The dispatch asked to confirm 3 identically-shaped mask-strip loops and name one shared serializer. Confirmed the shape (near-byte-identical `buildSnapshot()`, diffed — only whitespace-alignment + `cloud-project-manager.js` adding one `thumbnail` field differ) — but **2 of the 3 sites live in files that are themselves entirely DEAD**: `grep`, repo-wide, for any importer of `preset-manager.js` or `cloud-preset-manager.js` → 0 hits. `cloud-project-manager.js`'s OWN header says so explicitly: `"Replaces both preset-manager.js (localStorage) and cloud-preset-manager.js (inline cloud UI)."` So the real fix isn't "extract 3 call sites into 1 serializer" — it's "delete 2 whole dead files (283 lines), and the triplication mostly vanishes with them." The one LIVE copy (`cloud-project-manager.js:64-65,669`) is still worth declaring alongside A5a-1's fix (both are "clone P, strip mask" — could be the same function), but that's a much smaller, single-file cleanup once the dead pair is gone. | `grep -rln "from.*preset-manager\.js\|from.*cloud-preset-manager\.js" --include=*.js --include=*.html .` (repo-wide) → 0 hits. Read `cloud-project-manager.js:1-15` (the header naming both as replaced). | delete both files (0 importers, self-documented as superseded); fold `cloud-project-manager.js`'s `buildSnapshot` + A5a-1's `takeSnapshot` fix into one declared "clean P for persistence" helper | M (283 lines of confirmed-dead, zero-risk-to-delete code; reclassifies rather than just restates the dispatch's premise) |
| A5a-3 | b-spline-gen.py | `b-spline-gen.py:40` (`handlers = []`) / `:1592-1634` (`stop()`) | bug / P2-violation | `handlers` is declared once at module scope and **never cleared anywhere in the file** — `grep -n "handlers.clear\|handlers = \[\]"` → only the declaration, 0 clears. Every other add-in in this suite clears its handlers list in `stop()` (usually in a `finally`, confirmed by direct read in A1/A4: `bspline-frame-builder.py:768`, `template-maker.py:857`, `stamp-editor.py:1432`) — `b-spline-gen.py` is the one exception. `stop()` (`:1592-1634`) does delete the palette and command definition (which should release the FUSION-side event bindings for `onHTMLEvent`/`onClosed`/`onExecute`/`onCommandCreated`), but the PYTHON-side `handlers` list itself keeps every one of those handler objects referenced forever, growing by ~1-4 entries on every Stop→Start cycle for the life of the Fusion session — a session-scoped Python object leak on the add-in's own primary hot-reload workflow. Not covered by the parent's teardown either: `bspline-frame-builder.py`'s `_teardown_submodules` (read in A1) only calls `_bs.stop(None)` for b-spline-gen — it does NOT touch `_bs.handlers` directly the way it does for `_fb_sketch.handlers`/`_fb_solid.handlers`. | `grep -n "handlers.clear\|handlers = \[\]" b-spline-gen.py` → 1 hit (the declaration only). Cross-referenced against the 3 other add-ins' confirmed-clearing `stop()` patterns from earlier turns. | add `handlers.clear()` to `stop()`, matching every sibling add-in's pattern | M (memory leak, not data-loss; UNVERIFIED at runtime how large this grows over a real dev session, but the missing clear is unambiguous static fact) |
| A5a-4 | (scope-boundary note, not a new finding) | `core/state.js:253-254` vs `editor/editor-io.js:27-49` | correction | NEXT-SESSION's item 5 asks "B6 still present? state it plainly with the line" at `core/state.js:253-254`. **That line is NOT B6.** It's `saveLastSession`'s mask-strip (the SAME benign, explicitly-regenerable pattern A5a-1/A5a-2 concern — nulls `.mask`, keeps `.svg`/`.enabled`/everything else, for EVERY layer regardless of visibility, purely to avoid the JSON/localStorage bloat). B6's actual, BUGS_OPEN-described mechanism — `_visibleContent` permanently deleting a HIDDEN layer's geometry at save — lives in `editor/editor-io.js:27-49`, which is **A5b's scope, not A5a's** (`editor/` wasn't read this turn). Flagging the citation mismatch now so A5b checks the right file rather than also landing on `state.js:253-254` and concluding B6 is fixed when it was never the right site to begin with. | Read `core/state.js:238-260` (`saveLastSession`) in full — confirmed it strips `.mask` only, unconditionally, not gated on layer visibility. Grepped `core/`/`main/` for `_visibleContent` → 0 hits (confirms the real B6 site is genuinely outside A5a's files). | (none — routing note for A5b, not a code fix) | — |
| A5a-5 | core/fusion-bridge.js | `core/fusion-bridge.js:41-51` (`sendFusionPreview`) | bug (B4-class dead send) | `sendFusionPreview(preview)` sends action `'preview'` (`:47`) — called from `export-flow.js:92` (`if (isFusionMode) sendFusionPreview(preview)`, the "final high-fidelity preview before export" step). `b-spline-gen.py`'s dispatcher has **zero branch for `'preview'`** — only `'preview_mesh'`, a different action name entirely. Not silent-silent: since `'preview'` isn't in the high-frequency exclusion list (`:689`, `['check_import_status','ping','preview_mesh','log']`), it DOES get logged (`_log(f'Action: "preview" \| Data: ...')`, `:690`) — but nothing after that line processes it. The mesh data is sent, logged, and dropped. Same class as the already-tracked **B4** (`import_svg_sketches`), different action/call-site — not a duplicate of B4, a sibling instance. | `grep -c "'preview'"` (exact, closing-quote-bounded so it doesn't match `'preview_mesh'`) across every `if/elif action ==` branch in `b-spline-gen.py` → 0. Traced the JS send site (`fusion-bridge.js:47`) to its only caller (`export-flow.js:92`) and confirmed `isFusionMode` gates it, so it's Fusion-mode-only, real traffic when it fires. | either wire a Python receiver for `'preview'`, or fold it into the already-handled `'preview_mesh'` action if that's the intended live path and `sendFusionPreview` is meant to be retired | M (same confidence class as B4 — traced call site to dispatcher exhaustively, not a survey guess; severity M not H since it's a pre-export preview refresh, not the export itself, which uses the separately-confirmed-working chunked-payload path) |

## P1 sweep (item 1) — reconciled against B9/B11, no NEW violations

Grepped `main/` + `core/` for `isFusion`, `adsk\.`, `window\.fusion`, `fusionSendData`, `location\.host`
outside `fusion-bridge.js` → 6 files matched. **3 are false positives, ruled out by reading the code, not
just the grep hit:** `main/export-flow.js`, `main/header-controls.js`, `core/engine/rebuild.js` all
branch on the `isFusionMode` STATE FLAG (a plain boolean, not host behavior) to decide WHEN to call a
function — and every one of those functions (`sendFusionPreview`, `sendFusionMeshPreview`,
`sendFusionPayloadChunked`, `startFusionPolling`, `stopFusionPolling`, `fusLog`) is imported FROM
`core/fusion-bridge.js` (confirmed via each file's own `import` block). That's the CORRECT architecture
— business logic asks "am I in Fusion mode?" and calls the bridge's export; it doesn't re-implement host
I/O. **The remaining 3 hits are the 2 already-tracked violations, confirmed still present at current
line numbers, nothing new:**
- `main/main.js:136` — `adsk.fusionSendData('get_design_params', '{}')` directly, matches **B9** exactly.
- `core/coords.js:14-15` and `core/state.js:264,266` — direct `adsk.fusionSendData('log', ...)` calls,
  matches **B11** exactly (the log-tunnel re-inlined outside `fusion-bridge.js`).

## Doorless handlers, JS→Python direction (item 6, partial)

Enumerated every `adsk.fusionSendData('<action>', ...)` call across `core/`+`main/` (8 distinct action
strings: `check_import_status`, `generate_chunk`, `generate_finish`, `generate_start`,
`get_design_params`, `log`, `preview`, `preview_mesh`) and checked each against `b-spline-gen.py`'s
dispatcher. **7 of 8 have a matching branch. 1 does not — see A5a-5 above** (`'preview'`, sent by
`fusion-bridge.js`'s `sendFusionPreview`, no Python receiver). Python→JS direction not done this turn —
see "what I could not verify."

## Inefficiencies (item 7)

**Rebuild IS properly debounced — the dispatch's concern is unfounded here, confirmed by reading the
scheduler, not assuming.** `core/engine/scheduler.js` (23 L, read in full): `scheduleRebuild()` does
`clearTimeout` + re-`setTimeout` (default 50ms) on every call — a slider drag firing many param-change
events collapses to ONE rebuild ~50ms after the last event, not one per tick. `main/param-manager.js`
routes every param change through this scheduler (`:106,121`), never a direct synchronous `rebuild()`
call from an input handler. **No O(n²) found in `core/engine/rebuild.js`** (304 L) — the one grid loop
found (`:180`, `for k < nx*nz`) is a single O(n) pass over the flattened height array, not nested. Did
NOT do a full O(n²) sweep of `core/preview/*.js` (terrain-mesh.js 438L, sculpt-controller.js 577L) —
out of this turn's time budget, flagging as unchecked rather than clean.

## Test gap (item 8) — which findings land in tested vs untested code

The 4 vitest files import `core/state.js`, `editor/editor-coords.js`, `main/stamp/_shared.js`,
`main/app-init.js`, `main/stamp/svg-source.js`. Cross-referencing:
- **A5a-1 (`core/history.js`, `takeSnapshot`) — UNTESTED.** `history.js` isn't imported by any test file.
- **A5a-2 (dead files) — untested, and now known why: nothing imports them, tests included.**
- **A5a-3 (`b-spline-gen.py`) — untestable by this suite at all.** It's Python; vitest only covers the
  JS side, and (per STANDARDS-AUDIT §2, reconciled in A1) there is no Python test coverage for any
  add-in's lifecycle (`run`/`stop`) anywhere in the repo.
- **B9/B11 sites — the FILES (`core/state.js`) are imported by tests, but not necessarily these LINES.**
  `state.js` is 384 lines; the vitest suite likely exercises specific exported functions, not
  `saveLastSession`'s internal `adsk.fusionSendData` branch at `:264,266` — did not confirm which
  specific `state.js` exports the 4 test files actually call, so this is a "file is touched, line
  probably isn't" claim, not a confirmed line-level coverage check.

## What I could not verify (A5a)

- A5a-1's downstream-silent-skip claim (mask corruption surviving to a live `rebuild()` call) — the
  shape-corruption itself is certain from the code; whether a real user sequence actually hits
  `hasStampSvg === false` at the moment of undo/redo (vs. the far more common case where it's `true` and
  the regen masks the bug) needs either a live-Fusion/browser repro or a closer trace of every path that
  can set a layer's `.svg` to falsy — not done this turn.
- **Exports-with-0-importers (item 3's second half) — only the 2 named dead files were confirmed; no
  exhaustive sweep of all exports across ~13k lines of `core/`+`main/` was performed.** This was an
  explicit scope-vs-time tradeoff: the named candidates got a thorough, evidence-backed answer; a
  full export/importer graph across every file did not fit this turn's budget. Flagging as a real gap,
  not silently skipping it.
- `core/preview/*.js` (terrain-mesh.js, sculpt-controller.js, orbit-controller.js — 1300+ combined
  lines) — not read for O(n²) patterns; only `rebuild.js` itself was checked.
- Declared-vs-hand-rolled beyond the mask-strip triplication and the P1 sweep — item 2's "duplicated
  literals (ids, storage keys, API paths)" half wasn't separately swept this turn.
- Python→JS direction of the doorless-handler sweep (item 6's other half) — checked JS→Python fully (see
  A5a-5 below, found via that exact sweep) but did not cross-check every `_log`/`sendInfoToHTML`-style
  Python→JS event against a JS-side listener; `main/`'s `sendInfoToHTML`/incoming-event handling wasn't
  enumerated the way the outbound `fusionSendData` sites were. **Done this turn — see A5b, item 3.**

---

# A5b — b-spline-gen editor + palette HTML

**A5a review note:** advisor accepted A5a, including the B6-citation correction — "that is exactly the
second-observer value." A5a-1/-2/-3 become one seat-A task (delete the dead pair, declare one shared
"P for persistence" serializer, add `handlers.clear()`); A5a-5 is being decided by the advisor; B9/B11
queued. Nothing to re-verify this turn.

**Scope read this turn:** `editor/editor-io.js` (full, the B6 site), `editor/layers.js:75-145` (undo-
bundling), all `pushState()` call sites across `editor/*.js` (20 total; traced the 4 in
`editor-interaction.js` — the file B1 originally concerned — to their containing functions), the
`OPEN_SHAPES` constant in `editor-expand-shape.js` (B3), an exhaustive export/importer sweep across
`editor/`'s 100 top-level exports, `index.html` (6 lines, full) vs `bspline_gen_palette.html` (1979
lines — read the 3 inline `<script>` blocks + skimmed the 3 inline `<style>` blocks' boundaries),
`b-spline-gen.py`'s 7 `sendInfoToHTML` event names against every `main/`+palette-HTML listener, and the
16 `fm*` ids `cloud-project-manager.js` reads against the HTML.

## B6 — RESOLVED. Stated plainly, with the real mechanism and every caller.

**The function NEXT-SESSION pointed at (`_visibleContent`) no longer exists anywhere in the codebase**
(`grep -rn "_visibleContent" editor/ main/ core/` → 0 hits). It has been fully replaced by
`serializeEditor` (`editor/editor-io.js:36-40`), whose own docstring (`:23-30`) names B6 explicitly:
*"Hidden layers are intentionally KEPT... Dropping their geometry here loses it permanently on
save/reopen and on file download (B6)."* This is a completed fix (labeled "EDM3" in the comment),
not a still-open bug. **Every caller, traced:**
- `save()` (`editor-io.js:203-217`) — the primary persistence path. Calls `serializeEditor(editor)`
  with NO `forRaster` flag, i.e. the full-hidden-layers-included path. Its own inline comment
  (`:205-208`) restates the B6 fix in the exact same words as the function docstring.
- `saveForRasterization()` (`editor-io.js:293-`) — the font-embedding save variant used by the
  stamp/rasterization pipeline. Same call shape, same full-layers behavior.
- `getLayerSvg()` (`editor-io.js:99-`, calls `serializeEditor(editor, {forRaster:true})` at `:109`) —
  this IS visibility-filtered, but that's **intentional and correct**, per the docstring's own note:
  *"The stamp/rasterize exclusion of hidden layers lives in getLayerSvg (per-visible-layer), NOT here."*
  This is a DIFFERENT concern (what gets shown in the live
  3D stamp preview) from B6 (permanent data loss on save) — not a regression, the two were always meant
  to differ.

**Verdict: B6 is fixed. Both actual persistence functions keep all layers' geometry; only the
preview-specific renderer filters by visibility, which was always correct behavior.**

## B1/B3 (undo) — reconciled against current code, not the July doc

**B1 (Ctrl+Z multi-erase): still holds as "likely-FIXED," re-verified against a MUCH larger surface
than July's T2 traced.** `pushState()` now has **20 call sites** across `editor/` (was effectively 1
traced site in July — `finishDrawing`). Rather than re-cite the old verdict, traced the cluster most
relevant to B1's original symptom: the 4 sites in `editor-interaction.js` (`:143,211,428,639`) map to 4
DISTINCT, mutually-exclusive user gestures — `_pasteFromClipboard` (paste), `handleEnd` (drag-release,
gated on `editor._dragMoved`), `_commitAnchorPath` (pen-tool path completion), and `finishDrawing`
(freehand stroke — the ORIGINAL July site, same line number, `:639`, confirming this specific call
survived unchanged). None double-fire for the same single gesture. `layers.js`'s `addLayer` (`:136`)
correctly uses `skipUndo:true` when bundling layer-auto-creation into the SAME undo step as the
triggering stroke (`ensureActiveLayer`, `:84-94`) — this is intentional bundling, not a bug, and matches
the comment's stated intent exactly. **Did not trace all 20 sites** (layers.js's other 3, text-session/
text-style's 3, eraser's 1, expand-commit's 1, io's 2, properties-shape's 1) — flagging as unchecked
rather than extending the "fine" verdict past what was actually read.

**B3 (Expand on lines): unchanged, still likely-fixed-at-dispatch.** `OPEN_SHAPES = ['path', 'polyline',
'line']` (`editor-expand-shape.js:42`, was `:41` in July — one-line shift, same content). Output
correctness is still runtime-only, as T2 already said; nothing new to add.

## Python→JS dead sends/listeners (item 3) — 2 dead sends found

Enumerated all 7 `pal.sendInfoToHTML('<event>', ...)` names in `b-spline-gen.py` against every JS
listener in `main/`+the palette HTML. **5 of 7 wired correctly** (`build_info`, `import_ready`, `pong`,
`reset_ui`, `sync_board` all have a matching `action === '...'` branch, mostly in `main.js:156` and
nearby). **2 are dead sends, same class as B4:**
- **`import_progress`** (`b-spline-gen.py:180-187`, `_send_progress(msg)`) — called from **8 sites**
  throughout the STEP-import workflow (`:914,991,1079,1293,1304,1310,1314` — "Preparing Geometry...",
  "Importing X...", "Analyzing Stamping Surface...", etc.). Zero JS handling anywhere. This is LIVE,
  frequently-firing traffic during a real user-facing operation (STEP import back into Fusion) that
  produces zero visible progress feedback — a genuine UX gap, not just dead code.
- **`import_success`** (`b-spline-gen.py:1319`) — sent once at import completion. Also zero JS handling.

No dead LISTENERS found in the other direction (every JS `action === '...'` check in `main.js` matches
a real Python send) — checked while doing this sweep, not a separate pass.

## editor/ (item 4)

**P1: CLEAN.** `grep -rln "isFusion|adsk\.|window\.fusion|fusionSendData|location\.host" editor/*.js` →
0 hits. The SVG editor has zero host-specific branches — matches P1 exactly.

**C2 migration (SVG.Point.transform survivors): CLEAN.** `grep -rn "new SVG.Point\|SVG\.Point("
editor/*.js` → 1 hit, and it's a COMMENT in `editor-coords.js` documenting what `transformPoint` (the
declared replacement) exists to replace — zero actual hand-rolled usages remain.

**Exhaustive 0-importer export sweep: 1 confirmed dead, 5 over-exported.** Extracted all 100 top-level
`export function/const/class` names from `editor/*.js`, checked each against the whole `html/` tree.
- **`createButton`** (`editor/dom.js:45`) — TRULY dead: appears nowhere else at all, not even used
  within its own file beyond the definition.
- **`dismissExpandCallout`, `initTextSession`, `removeLayer`, `renameLayer`, `reorderLayer`** — a
  different, smaller finding: each IS used, but only WITHIN its own defining file (2-3 occurrences:
  definition + local call site(s)); nothing outside ever imports them. Not dead code — just exported
  wider than needed, implying a public API surface that doesn't exist. Distinguishing this from
  `createButton`'s true deadness matters: conflating the two would overstate 5 of the 6 hits.

## `bspline_gen_palette.html` vs `index.html` (item 5) — corrects the dispatch's own framing

**There is no "web host vs Fusion host" pair of HTML files to diff.** `index.html` is a **6-line meta-
refresh redirect stub** (`<meta http-equiv="refresh" content="0; url=bspline_gen_palette.html">`) — not
a second implementation. Confirmed why: `b-spline-gen.py:141` points the Fusion palette DIRECTLY at
`PALETTE_HTML = 'html/bspline_gen_palette.html'`, bypassing `index.html` entirely; `index.html` only
exists so the WEB deploy's root URL (`https://bspline-generator.pages.dev/`) resolves to something
without needing the full filename in the address bar. **There is exactly ONE real page**, serving BOTH
hosts at runtime via `isFusionMode`/`isFusion()` checks (already traced in A5a) — which is actually P1's
architecture taken further than a literal reading requires: not just "host logic confined to one file,"
but one HTML entry point, period. Nothing to diff because there's nothing to diverge.

**One genuine P1 nuance found, not caught by A5a's `.js`-only grep (it never looked inside HTML):** the
palette HTML's inline `<script>` at `:1162-1193` defines `window.fusionJavaScriptHandler.handle` — the
actual RECEIVER for Fusion's `pal.sendInfoToHTML(...)` calls (re-dispatched as a `CustomEvent` for ES
modules to listen to). This is host-bridge logic living OUTSIDE `core/fusion-bridge.js`, which by the
letter of P1 is a violation. **But it's evidenced as a NECESSARY exception, not an oversight** — the
comment at `:1163-1172` explains Fusion's palette API requires this exact global function name to exist
synchronously; an ES module (deferred, async-loaded) can't guarantee that. Flagging as a **named
exception P1 should document**, not a bug to fix — "fixing" it by moving it into a module would likely
BREAK the early-availability guarantee it depends on.

**Inline `<style>` volume, not fully deduped this turn:** 3 blocks, ~576 combined lines
(`:33-251`, `:1433-1532`, `:1636-1895`) — did not cross-check for duplication against `styles/base.css`
etc.; flagging the scale as worth a follow-up look, not asserting duplication without having checked.
One inline `<script>` (`:1060-~1160`, `togglePanel` + a `DOMContentLoaded` "Native CAD UI Integration
Patch" bridging legacy IDs to `main.js` logic) is genuine UI logic living in HTML rather than `main/` —
its own comment frames it as a deliberate bridge, not accidental duplication, but wasn't read past its
opening to confirm nothing in it duplicates a `main/` function.

**PM1 id check: 15 of 16 exist exactly once. 1 does not.** Extracted every `fm*` id
`cloud-project-manager.js` reads via `getElementById` (16 total) and counted each in the palette HTML.
15 match exactly once (`fmBreadcrumb`, `fmBtnDelete`, `fmBtnLoad`, `fmBtnNewFolder`, `fmBtnRename`,
`fmBtnSave`, `fmBtnSaveAs`, `fmCloseBtn`, `fmProjectList`, `fmProjectMsg`, `fmProjectName`,
`fmProjectStatus`, `fmSelbarInfo`, `fmViewToggleList`, `fmViewToggleTiles`) — PM1/PM1b's restoration
held. **`fmCurrentFileLabel` (read at `cloud-project-manager.js:887`, inside
`updateHeaderFileIndicator()`) does not exist anywhere in the HTML.** Correctly null-guarded (`:888`,
`if (!el) return;`) so this is a SILENT no-op, not a crash — but the "current file" indicator this
function exists to drive never appears anywhere in the UI. Whether this element was ever actually in the
HTML (and got dropped in some earlier edit, unrelated to PM1 since PM1 only restored the selection-bar
block, not a header indicator) or was designed but never wired in wasn't traced further this turn.

## New findings table (A5b)

| ID | area | file:line | class | severity |
|---|---|---|---|---|
| A5b-1 | b-spline-gen.py / main.js | `b-spline-gen.py:180-187,1319` (`_send_progress`, `import_success` send) | bug (B4-class dead send) — 8 live call sites during real STEP-import, zero JS handling; real UX gap (no progress feedback) | M-H |
| A5b-2 | editor/dom.js | `editor/dom.js:45` (`createButton`) | dead — 0 references anywhere, not even locally | L |
| A5b-3 | editor/ (5 functions) | `editor-ui.js:54`, `editor-text-session.js:106`, `layers.js:140,165,179` | over-exported (used only within own file) — not dead, but a misleading public-API surface | L |
| A5b-4 | bspline_gen_palette.html | `:1162-1193` (`window.fusionJavaScriptHandler`) | P1 exception, justified — should be documented as a named exception, not silently left ambiguous | L (documentation gap, not a bug) |
| A5b-5 | bspline_gen_palette.html / cloud-project-manager.js | `:887-894` reads id `fmCurrentFileLabel`, absent from HTML | dead UI wiring — silent no-op, the "current file" indicator never renders | L |

## Test gap (item 6)

The 4 vitest files touch exactly ONE file in this turn's scope: `editor/editor-coords.js`
(`transformPoint`, `carveMatrix` — both confirmed clean, no findings there). **Every other file audited
this turn is untested:** `editor/editor-io.js` (the B6 fix itself — `serializeEditor`/`save`/
`saveForRasterization` have zero test coverage), `editor/layers.js`, `editor/editor-interaction.js` (the
B1-relevant pushState cluster), `editor/dom.js`, both HTML files (untestable by vitest as authored — no
DOM/jsdom harness in this suite per the import graph), and `b-spline-gen.py` entirely (Python, no test
harness exists per A1/A5a's reconciliation). The one fix this turn confirms as DONE (B6) has no
regression test guarding it — a behavior-preserving refactor of `serializeEditor` could silently
reintroduce B6 with nothing to catch it.

## What I could not verify (A5b)

- B1: only the `editor-interaction.js` 4-site cluster was traced to confirm no double-fire; the other
  16 `pushState()` call sites (layers.js ×3 more, text-session/text-style ×3, eraser, expand-commit,
  io ×2, properties-shape) were not individually checked for the same concern.
- A5b-5's root cause (`fmCurrentFileLabel`'s history — dropped, or never built) — not traced via git
  history; stated only what's true of the CURRENT tree.
- Inline `<style>` blocks (~576 lines) — flagged for volume, not checked for actual duplication against
  `styles/*.css`.
- The inline `togglePanel`/`DOMContentLoaded` bridge script (`:1060-~1160`) — read its opening comment
  and purpose, not its full body against `main/`'s own DOM-binding code for duplication.
- Whether `main/ui-bindings.js` (147 L, not read this turn) already does what the inline bridge script
  does, which would make the inline block a THIRD kind of duplication beyond A5a-2's pattern — flagged
  as a real possibility, not confirmed.

---

# A6 — CAM-builder

**A5b review note:** advisor accepted A5b. B6 closed on the evidence this lane provided. A5b-1
(the import-progress dead sends) becomes a seat-A task (wire the listener). The named-exception P1
finding (`window.fusionJavaScriptHandler`) will be documented in ROADMAP as such. Nothing to re-verify.

**Scope read this turn:** `cam-builder.py` (2315 L — full lifecycle read of `run()`/`stop()`/
`_register_refresh_event`/`_load_engine`, plus targeted greps for the rest), `cam_engine/` (5 files,
4588 L — targeted: `setup_builder.py`'s machine-coupling + a loop-nesting survey, `mm_builder.py`'s
body-enumeration loop, `SETUP_SPECS`/table-shaped declarations), `cam_utils/` (2 files, 247 L, grepped),
both palette HTML files (`ui/html/{cam_builder,cam_studio}_palette.html`, 1713 L combined — full
doorless-handler sweep, both directions, both palettes) — the deepest doorless sweep this lane has done,
given CAM-builder has 2 separate palettes each with their own dispatcher.

## B10 (item 1) — STILL TRUE, confirmed with current lines

**Exactly matches BUGS_OPEN's description, re-verified against current code, not assumed still
accurate.** Three CustomEvents registered in `_register_refresh_event()` (`:2026-2075`, called from
`run()`): `REFRESH_EVENT_ID` (`:2041`), `TPGEN_EVENT_ID` (`:2054`), `AXISPICK_EVENT_ID` (`:2066`).
`stop()` (`:2211-2298`) unregisters **only `REFRESH_EVENT_ID`** (`:2284`) — `TPGEN`/`AXISPICK` are never
touched there. **Confirmed the masking mechanism B10 already describes still holds:**
`_register_refresh_event()` itself does `unregisterCustomEvent` for ALL THREE before re-registering
(`:2037,2051,2063`) — so a normal Stop→Start cycle is safe regardless of what `stop()` skipped; the
residual risk is specifically a `stop()` not immediately followed by a `run()`. No new evidence changes
B10's severity assessment (LOW, per BUGS_OPEN) — this turn just re-grounds it in current line numbers
rather than trusting the July citation.

**Lifecycle symmetry, broader than B10:** CAM-builder runs 2 palettes (`PALETTE_ID`/`STUDIO_PALETTE_ID`)
+ 2 commands (`CMD_ID`/`STUDIO_CMD_ID`). `stop()`'s steps 1-3 (`:2231-2279`) handle both symmetrically —
palette delete, control removal across all matching panels, command-def removal, all in paired loops
over `(PALETTE_ID, STUDIO_PALETTE_ID)`/`(CMD_ID, STUDIO_CMD_ID)`. **No `activeSelectionChanged` or
`documentActivated` subscriptions exist anywhere in this file** (grepped, 0 hits) — so CAM-builder has
no B5-class app-level-subscription-leak surface at all; the ONLY lifecycle gap is B10's already-known one.

## Duplication with fb_shared / frame-builder (item 2) — CLEAN

**Zero duplication found, either direction.** CAM-builder imports exactly one thing from `fb_shared`:
`build_info` (`cam-builder.py:1191`, the same version-stamp pattern every other sub-add-in uses) — no
import of `entity_helpers`/`expression_coords`, and grepping for reimplementations of their signature
functions (`get_fb_name`, `_get_arc_midpoint`, `get_entity_coord`, FrameBuilder-attribute reads) across
all of `CAM-builder/` → 0 hits. This makes sense on inspection: CAM-builder's domain is Fusion CAM
setups/operations/bodies, not sketch entities carrying FrameBuilder attributes — there's no shared
concept to duplicate. No import of `frame-builder`'s `fb_engine` either.

**Hand-rolled tables: none of the kind asked about found.** Grepped for feed/speed/RPM/chipload/tool-
library/post-processor literals across `cam_engine/` → 0 hits — CAM-builder configures Fusion's OWN
CAM operations (which own feeds/speeds/tooling via Fusion's library, referenced by name/id) rather than
reimplementing that math. The one large declared-table candidate found, `SETUP_SPECS`
(`cam_engine/setup_builder.py:822`), is already the CORRECT declare-over-hand-roll shape (a list of
setup specs), not a violation to fix.

**Dead functions: 0 found, at the scope checked.** Extracted all 33 top-level `def` names in
`cam-builder.py` and checked each against the whole `CAM-builder/` tree (Python files + both palette
HTMLs, since some are called from JS-triggered dispatch) — every one has ≥1 caller beyond its own
definition. **Did not extend this check into `cam_engine/setup_builder.py` (2422 L) or `mm_builder.py`
(1489 L)** — the two largest files, and the most likely place a dead function would hide — flagging as
the honest scope limit rather than implying full coverage.

**Honesty: clean.** Grepped for "standalone add-in"/"used to be"/"previously"/"legacy" framing that
might describe a pre-consolidation state no longer true → the 2 hits found are both accurate, unrelated
uses (a `'generate'` action alias comment, a defensive-parsing comment) — no stale claims.

**One reconciled item, amplified beyond its original citation:** STANDARDS-AUDIT's hardcoded-path finding
(`cam_engine/setup_builder.py:206`, the `Ultimate Bee 3 axis.mch` absolute path) is confirmed **still
present, verbatim**. But the single-line citation understates the coupling: `"Ultimate Bee"` appears
**~15 times** throughout `setup_builder.py` (`:32,183,206,233,237,287,497,527,546,551,920,942,1728,1805,
2037`) — machine-name matching, sim-doc detection, default machine assignment, fence-anchored WCS
derivation. This isn't one portable-path landmine; the whole setup-builder pipeline assumes this specific
physical machine. Not a new finding (STANDARDS-AUDIT §5 already covers the class), but worth stating the
actual scope for whoever picks up the portability fix — a single-line patch at `:206` wouldn't address it.

## Doorless handlers (item 3) — CLEAN, both directions, both palettes — the best result this session

**JS→Python: 13 distinct actions across 2 palettes, all wired.** `cam_builder_palette.html`'s 8
(`add_machine`, `apply_toolpaths`, `build`, `get_template_assignments`, `list_cam_templates`, `preview`,
`set_template_assignments`, `sync_table_attach`) + `cam_studio_palette.html`'s 7 (`generate`,
`import_setup`, `init`, `preview`, `preview_clear`, `select_x_axis`, `select_y_axis` — the last two sent
via a ternary, initially missed by a literal-string grep and caught by cross-checking the count mismatch
rather than trusting the first pass) — every one has a matching `elif action == '...'` branch in
`cam-builder.py`'s two dispatchers (`:249-267` for the builder palette, `:436-448` for studio).

**Python→JS: 7 distinct events across 3 send-helpers, all wired.** `_palette_send`/`_send_to_studio_html`/
`_send_to_html` (traced each helper to its callers rather than grepping literal `sendInfoToHTML(` calls,
since all 3 are parameterized by an `action` variable) send `template_assignments`, `templates_list`,
`build_info`, `preview`, `report`, `axis_picked`, `import_result`, `init_result` — every one has a
matching listener in the CORRECT target palette (cross-checked which helper targets which `PALETTE_ID`).
**Zero dead sends, zero dead listeners** — unlike b-spline-gen's 3 confirmed dead sends (A5a-5, A5b-1)
or the earlier E7a/B4 pattern, CAM-builder's bridge discipline is fully intact.

## Cross-sub bare-name collisions (item 4) — ZERO. Clean, definitive answer.

**CAM-builder imports no bare module names at all, at any level.** The entry (`cam-builder.py:112-119`)
loads its engine PACKAGE-qualified (`from cam_engine import cam_coordinator`, `from cam_utils.cam_logger
import make_logger`) — never a bare `import setup_builder`-style name. Grepped every import statement
inside `cam_engine/*.py` + `cam_utils/*.py` too (not just the entry) → same result, 100% package-qualified
or stdlib/`adsk.*`. Cross-checked the submodule FILENAMES themselves against `template-maker/core/`'s
(the other sub-add-in with the most bare-name history, per A3) — `comm -12` on the sorted name lists →
0 overlaps. **No `cam_engine`/`cam_utils` directory exists anywhere else in the repo either** (checked
`frame-builder/`, `template-maker/`, `stamp-editor/`) — no top-level package-name collision risk.
**Answer to the framing question: CAM-builder contributes ZERO reasons to keep the parent's
`_shared_project_names` list.** (The list still can't be fully retired on CAM-builder's evidence alone —
template-maker's OWN bare imports, per A3, are the actual reason it must stay/derive; this turn only
rules CAM-builder out as a contributor, it doesn't settle the whole question.)

## Inefficiencies (item 5) — no O(n²) or excessive COM-read pattern found, at the depth checked

CAM-builder's domain is Fusion CAM SETUP/OPERATION configuration, not raw toolpath point math — the
actual toolpath geometry generation is Fusion's own CAM engine's job, reached through the API, not
reimplemented here. Surveyed the loop nesting across `mm_builder.py` + `setup_builder.py`: the deepest
nesting found (`setup_builder.py:231-268`, documents→products→occurrences→childOccurrences, 4 levels)
walks a FIXED, small tree (one simulation document's structure) for a one-time diagnostic/setup scan,
not a per-point hot loop — bounded by document/occurrence counts, not by geometry resolution.
`mm_builder.py`'s body-enumeration generator (`:763-776`) is a single O(n) pass over the design's bodies
(typically a handful of frame parts), reading `.name` once per body. `.geometry` COM-accessor count
across all of `cam_engine/`+`cam-builder.py`: 8 total occurrences — no sign of a repeated-read-per-
iteration pattern. **Did not exhaustively trace every loop in the 2422-line `setup_builder.py`** — spot-
checked the deepest nesting found via grep and the two files most likely to hold point/geometry math;
flagging the depth limit rather than asserting a full clean bill.

## Tests (item 6) — 0, confirmed (matches A1)

`find . -iname "test_*.py" -o -iname "*_test.py"` under `CAM-builder/` → 0 hits. Matches A1's original
observation exactly; nothing has changed.

## What I could not verify (A6)

- Dead-function sweep was done only for `cam-builder.py`'s 33 top-level functions; `cam_engine/
  setup_builder.py` (2422 L) and `mm_builder.py` (1489 L) — the two largest, most likely candidates —
  were not swept the same way.
- Inefficiency check was a targeted survey (loop-nesting grep + `.geometry` count), not a line-by-line
  read of either 1400+/2400+-line engine file.
- `cam_engine/cam_coordinator.py` (268 L) and `cam_engine/cam_workspace.py` (144 L) — not individually
  read this turn beyond the top-level import graph; both are plausible homes for logic this turn's
  targeted searches could have missed.
- `parameter_introspect.py` (124 L) and `template_assignments.py` (123 L) — same, not individually read.

---

# A7 — cloud (preset-worker, step-editor cloud pair, deploy_cloudflare.py) — LAST audit slice

**A6 review note:** advisor accepted A6, cleanest add-in of the set. B10 becomes ticket HY3. Parent
wipe-list question settled: template-maker's own bare imports are the remaining reason it must
stay/derive; CAM-builder is ruled out as a contributor. Nothing to re-verify this turn.

**Scope read this turn:** `cloud/preset-worker/src/index.js` (567 L, full — the shared worker's main
router + the Appreciation Arts Plastiques GitHub-commit handler), `cloud/preset-worker/src/
pageviews-route.js` (277 L, full), `cloud/preset-worker/src/bus-route.js` (targeted: every PUT/DELETE
handler, ~150 L of the 507), `cloud/preset-worker/wrangler.toml` + `package.json`, `cloud/
step-editor-worker/src/index.js` (122 L, full) + its `wrangler.toml` + README, `cloud/step-editor-pages/
README.md` (full, it's the only file in that directory), `bspline-frame-builder/deploy_cloudflare.py`
(240 L, full — traced the `--build-only` control flow end to end for the staleness question).

## Route-by-route: auth / validation / size-cap / CORS (item 1)

**`preset-worker` serves 3 route families with very different security postures — stated plainly, not
averaged into one verdict.**

| Route family | Auth | Body validation | Size cap | CORS |
|---|---|---|---|---|
| `/projects`, `/presets`, `/cam-profiles` (PUT/DELETE) | **NONE** | JSON-parse only, name≤200 chars | 10 MB (`MAX_BODY_BYTES`) | `*` (open) |
| `/loader/apps` (PUT) | **NONE** | JSON-parse + shape check (`Array.isArray(.apps)`) | 10 MB | `*` |
| `/{projects\|palettes}` (penplotter, POST/PUT/DELETE) | **`X-API-Key`**, non-constant-time `!==` compare | none beyond `body.name` truncation | **none found** | `*` |
| `/views/*` (pageviews) | none (by design — public beacon), but Origin-allowlisted + bot-UA-filtered + botScore-gated | full shape validation (`screen()`) | 4 KB | `*` |
| `/bus/*` (6 PUT/DELETE endpoints) | **NONE** | JSON-parse only on 3 of 6; shape checks on `/places`, `/config` | **NONE — no `MAX_BODY_BYTES` anywhere in this file** | `*` |
| `/appreciation-arts-plastiques/commit` (POST, writes to GitHub) | **NONE** | filename regex + JSON-parse on sub-fields | none on the base64 image payload | `*` |

**Every unauthenticated WRITE, listed as asked (beyond the README's own admitted `/projects` case):**
`/presets/:name` PUT+DELETE, `/cam-profiles/:name` PUT+DELETE, `/loader/apps` PUT, `/bus/collecte-grid`
PUT, `/bus/collecte` PUT, `/bus/places` PUT, `/bus/config` PUT+DELETE, `/bus/streets` PUT,
`/appreciation-arts-plastiques/commit` POST (writes to a real GitHub repo), `/appreciation-arts-
plastiques/sound-import` POST (also commits to GitHub). **Combined with open CORS (`*`) on every route,
none of these need a same-origin trick to reach from a hostile page — any website, visited by anyone,
can script a `fetch()` straight at them.**

## New findings (A7)

| ID | area | file:line | class | severity |
|---|---|---|---|---|
| A7-1 | preset-worker | `bus-route.js` (6 PUT/DELETE handlers, no `MAX_BODY_BYTES` anywhere in the file) | security — missing size cap, unauthenticated | The 3 other route families in this same worker (`index.js`'s bspline routes, `pageviews-route.js`) both declare an explicit body-size cap; `bus-route.js` has none at all. An unauthenticated caller can `PUT` an arbitrarily large body to `/bus/collecte-grid` or `/bus/streets` (bounded only by Cloudflare's platform-level request-size ceiling, not by this worker's own code) — inconsistent with the discipline the sibling routes already establish. | M |
| A7-2 | preset-worker | `index.js:363-399` (`/appreciation-arts-plastiques/commit`) | security — unauthenticated write with an external, hard-to-revert consequence | Unlike the KV-backed routes (overwrite a value, trivially revertible), this route commits directly to a real GitHub repo using a server-side PAT — the comment at `:324-326` explicitly acknowledges the tradeoff ("No auth on this route... If abuse ever shows up, add an origin check or X-API-Key gate here"), so this is a KNOWN, accepted risk, not an oversight — but it's the highest-consequence unauthenticated write in the worker and deserves naming as such rather than being lumped in with the KV routes. No size cap on the base64 image payload either (Cloudflare's platform limit is the only ceiling). | M (accepted risk, but worth a periodic revisit — the comment itself invites one) |
| A7-3 | deploy_cloudflare.py | `:159-170` (dist prep) vs `:218-220` (early exit) vs `:233` (the only `clean_dir(deploy_dist)` call) | bug — confirmed via control-flow trace, not inferred | **`--build-only` mode's `dist/` is a pure, never-cleaned overlay.** `deploy_dist = "dist"` when `BUILD_ONLY` (`:159`); the only cleanup loop (`:161-166`) targets `deploy_dist_<timestamp>` and the LEGACY `deploy_dist` name — never the literal `dist` path. `os.makedirs(deploy_dist, exist_ok=True)` (`:170`) does not wipe existing content. `sys.exit(0)` fires at `:220` in BUILD_ONLY mode, BEFORE the only `clean_dir(deploy_dist)` call in the file (`:233`, which only runs in the non-BUILD_ONLY wrangler-deploy path). **Net effect: a file deleted from `b-spline-gen/html/` stays in `dist/` — and therefore stays live on the deployed Cloudflare Pages site — indefinitely across every `--build-only` rebuild.** Same root pattern as A1-6 (the Fusion AddIns overlay gap), now confirmed on the WEB deploy side too — worth naming as the second instance of the same class rather than a coincidence. | M-H (confirmed control-flow bug, real consequence for the public-facing site; not confirmed whether it has actually manifested — no way to check the live `dist/`'s history from this static read) |

## step-editor cloud pair (item 2) — precise 3-way status, not a single verdict

**`step-editor-worker`: code-complete, never deployed.** `src/index.js` (122 L, read in full) is a
clean, self-contained, well-validated implementation (STEP-file magic-header check on PUT, 25 MB cap,
proper CORS/error handling) — genuinely finished code, not a stub. `wrangler.toml` still has the literal
placeholder `id = "REPLACE_AFTER_KV_CREATE"` — the KV namespace was never provisioned, so this has never
actually been deployed. **`step-editor-pages`: README-only, confirmed matches STANDARDS-AUDIT §3
exactly.** The directory contains ONLY `README.md` — no `deploy.cmd`, no `html/` mirror. The README
references `bspline-frame-builder/step-editor/html/` as its source; that path does not exist anywhere
in the repo (checked). **The underlying Fusion add-in these two cloud pieces were built to serve,
`bspline-frame-builder/step-editor/`, does not exist at all** — and per `sync_stamp_bundle.py`'s own
comment (read in A4): *"step-editor used to ship the bundle for its in-house stamp engine; that engine
has moved to stamp-editor"* — i.e. the functionality this was for has already been absorbed by
`stamp-editor`. **Conclusion, stated plainly rather than hedged: this looks less like "half-built, needs
finishing" and more like a superseded initiative** — the add-in it would serve was folded into a
sibling before either cloud piece got provisioned. Worth a human decision (delete both cloud scaffolds,
or keep `step-editor-worker`'s finished code on ice) rather than treating it as a to-do.

## deploy_cloudflare.py (item 3) — dist staleness confirmed (A7-3 above); token handling clean

**Token logging: clean, confirmed by reading every reference.** `CLOUDFLARE_API_TOKEN` appears exactly
once in the file (`:69`, an existence check via `os.environ.get(var)` — never the value) — never printed,
never logged. Reconciles STANDARDS-AUDIT's "good hygiene" note for this specific file.

## Cloud-side hygiene (item 4)

**Clean on `.wrangler/`/`.bak`/lock-file drift** — none found (`find` for `.wrangler`, `*.bak` under
`cloud/` → 0 hits). `preset-worker` has a tracked `package-lock.json`; `step-editor-worker` has a
`package.json` but no lock file — consistent with "never had `npm install` run," which fits the
"never deployed" finding above, not a separate drift issue.

**STANDARDS-AUDIT §5's preset-worker identity-drift finding: HALF resolved, stated precisely rather than
as one verdict.** The dangerous half — `deploy_worker.py` (the orphan REST-deploy script that would have
published a mis-bound, wrong-name duplicate worker) — **no longer exists in the repo** (checked,
confirmed gone). The cosmetic half — `package.json:1-2`'s `name: "bspline-preset-worker"`, a third label
matching neither `wrangler.toml`'s `projects-dansemur` nor anything else — **is still present.** Since
npm's `package.json` name has no bearing on the actual Cloudflare deploy identity (only `wrangler.toml`'s
`name` does), the risky part of this finding is closed; the naming mismatch itself is now purely cosmetic.

## Dead routes vs live callers (item 1, continued) — corrected the framing, not just answered it

**Important scope correction before the answer: this worker is EXPLICITLY SHARED across multiple of
Fred's separate app repos** (confirmed via `wrangler.toml`'s comments and the route doc-comment at
`index.js:1` — "unified KV-backed project store for ALL of Fred's apps"). This repo (`b-spline-generator-
web-addin`) contains only `bspline-frame-builder/`, `cloud/`, `skills/`, `tests/` — no Nest Hub
bus-tracker client, no `triangle-frame-calc` frame-calculator site. So:
- **`/presets` — genuinely dead, and provably so WITHIN this repo.** Its only caller anywhere in this
  repo is `main/cloud-preset-manager.js` — which A5a-2 already confirmed is itself dead code (0
  importers). The live replacement, `cloud-project-manager.js`, calls `/projects` exclusively (checked
  every `fetch` call site). This is the one route I can call dead with real evidence, not just absence.
- **`/loader/apps`, `/bus/*`, `/views/*` — NOT verifiable as dead, and I'm not calling them that.**
  Zero callers found anywhere in THIS repo, but their `ALLOWED_ORIGINS`/naming (`triangle-frame-
  calc.pages.dev` for views; "Nest Hub bus tracker" per the worker's own git history) point at sibling
  apps that simply aren't checked out here. Reporting "0 callers in this repo" as "dead" would be a
  false claim this lane can't back up — said so explicitly instead of overreaching.
- **`/cam-profiles` — confirmed live**, called from `CAM-builder/ui/html/cam_studio_palette.html`.

## What I could not verify (A7)

- Whether A7-3's `dist/` staleness has actually manifested on the live deployed site (a file that's
  genuinely stale there right now) — this was a static control-flow trace, not a live check of the
  deployed Pages site's current file list.
- `/bus/*`'s missing input validation on 3 of 6 endpoints (`collecte-grid`, `collecte`, `streets` beyond
  the major/minor array check) — flagged the missing size cap (A7-1) but did not trace whether malformed-
  but-JSON-valid bodies could corrupt the downstream GET readers (e.g. `g.schedules.length` at
  `:92-94` assumes a shape the PUT handler never enforces) into a self-inflicted 502 — plausible, not
  confirmed with a concrete repro.
- The penplotter routes' non-constant-time API-key comparison (`key !== env.API_KEY`, `index.js:82`) —
  named as a timing-side-channel observation in the route table above, not promoted to its own finding;
  didn't assess real-world exploitability (a single personal API key, not a multi-tenant secret).
- Did not read `cam_studio_palette.html`'s actual `/cam-profiles` call sites in detail — confirmed the
  route is live via one grep hit, not a full trace of how it's used.

---

# Audit series summary (A1-A7) — every H/M finding, current disposition

One table, per the advisor's ask, to reconcile against ROADMAP. Severity as originally assigned in each
turn; disposition reflects what's known as of THIS turn (A7) — several were already actioned by seat A
mid-series, visible from the main-checkout activity this lane observed in passing (A1, A5a turns).

| Turn | ID | Finding (one line) | Severity | Disposition |
|---|---|---|---|---|
| A1 | A1-2 | Restates B5 (frame-inspector selection-handler leak) with tighter evidence | M | Open (tracked as B5) |
| A2 | A2-1 | Params created inside command Execute beyond `frame_tilt_deg` — contradicts UNDO-REDO-DESIGN.md's own scope | H (unverified) | Advisor verifying live (per A5a review note) |
| A2 | A2-3 | `isComputeDeferred` has no reset-on-exception path — silent-wrong-geometry risk | M-H | Dispatched to seat A as a declared context manager (per A5a review note) |
| A2 | A2-4 | Two builder UIs duplicate the hidden-command dispatch machinery (555/945 diff lines) | M | Queued, design-first (per A5a review note) |
| A3 | A3-1 | `_PROJECT_MODULES` missing 5 actively-imported names — B7-class stale-reload, wider blast radius | H (unverified) | Reframed as seat-A task: derive the list from `core/`'s file list, not just patch the 5 names (per A5b review note) |
| A5a | A5a-1 | `takeSnapshot` doesn't strip `.mask` before JSON round-trip — undo/redo corruption + perf cost | M-H | Folds into the seat-A "P for persistence" serializer task (per A5b review note) |
| A5a | A5a-2 | `preset-manager.js`+`cloud-preset-manager.js` — 283 lines confirmed dead | M | Seat-A task: delete both (per A5b review note) |
| A5a | A5a-3 | `b-spline-gen.py`'s `handlers` list never cleared in `stop()` | M | Seat-A task: add `handlers.clear()` (per A5b review note) |
| A5a | A5a-5 | `sendFusionPreview`'s `'preview'` action — dead send, B4-class | M | Advisor deciding (per A5b review note) |
| A5b | — | **B6 (hidden-layer data loss on save) — RESOLVED**, confirmed with full evidence and every caller traced | was H | **Closed** — advisor accepted this turn's evidence |
| A5b | A5b-1 | `import_progress`(×8 call sites)/`import_success` — dead sends, no progress feedback during STEP import | M-H | Seat-A task: wire the listener (per A6 review note) |
| A6 | (B10) | CustomEvent `stop()` releases 1 of 3 — reconfirmed still true, no severity change | L (per BUGS_OPEN) | Ticketed as HY3 (per A6 review note) |
| A7 | A7-1 | `bus-route.js` has no body-size cap on any of its 6 write routes | M | Open — new this turn |
| A7 | A7-2 | Unauthenticated GitHub-commit route — known/accepted risk, but highest-consequence unauth write in the worker | M | Open — accepted risk, named explicitly this turn |
| A7 | A7-3 | `deploy_cloudflare.py --build-only` never cleans `dist/` — same class as A1-6, now confirmed on the web side | M-H | Open — new this turn |

**Also worth carrying into ROADMAP, not severity-ranked but structurally important:**
- `step-editor-worker`/`step-editor-pages` — likely a superseded initiative (stamp-editor absorbed the
  functionality), not a to-do; needs a human keep-or-delete call.
- A1-6 (Fusion AddIns overlay gap) and A7-3 (Pages `dist/` overlay gap) are the SAME root pattern in two
  different deploy scripts — worth fixing as one lesson, not two unrelated tickets.
- Every add-in's B5-class lifecycle question is now fully mapped: frame-inspector (B5, open),
  template-maker (correct, the reference pattern), stamp-editor (correct, exemplary), b-spline-gen.py
  (A5a-3, open), CAM-builder (B10, open, low severity). The parent orchestrator itself (`bspline-frame-
  builder.py`) has clean run/stop symmetry (A1's "What's GOOD").
