/**
 * SE9: the preset swatch row for the per-element color control. Declared
 * once so the toolbar UI (properties-shape.js) has a single source to
 * build buttons from — never a second hand-typed list to drift from it.
 * The first four are Fred's own piece (red/yellow/navy/green); black and
 * white round out the set for outline-only and light-board work.
 */
export const VECTOR_COLORS = [
  '#000000', // black
  '#c62828', // red
  '#f9c80e', // yellow
  '#1a237e', // navy
  '#2e7d32', // green
  '#ffffff', // white
];
