# This folder is discovered by fb_engine.frame_engine via its ``template_``
# prefix and loaded through ``template_data.py``. The engine uses
# importlib's spec loader, so this ``__init__.py`` stays intentionally
# empty — nothing imports the package directly.
